# memory.md — Project State & Progress Tracker

> **Universal Intent Wallet (INTENT LAYER)** — build state, decisions, and session log.
> Requirements source of truth: [requirements.md](requirements.md). This file = what is DONE + what is NEXT.

## 🔁 Session Protocol (for every new Claude session)

1. Read this file first, then skim requirements.md §14 (Phase Plan).
2. Pick up at **▶ NEXT ACTION** below.
3. Build with production rigor (Charter rules, requirements.md §0). Run tests before claiming done.
4. Before ending: update Phase Status, Work Log, Decisions, and ▶ NEXT ACTION.

---

## 📊 Phase Status

| Phase | Name                                    | Status         | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ----- | --------------------------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0     | Foundation (monorepo, tooling, docs)    | ✅ Done        | pnpm, TS strict, Vitest, ESLint, Prettier, hooks, CI, coverage gate, infra baseline                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 1     | Core Wallet Engine — Universal Identity | ✅ Done        | `packages/core` — 126 tests. Identity + vault + FULL tx signing (EIP-1559/712/191, PSBT, SOL) + WalletManager/SigningManager/SessionManager                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2     | Chain Abstraction Layer                 | ✅ Done        | `packages/chains` — 69 tests. Registry + ProviderPool + full `BlockchainAdapter` (EVM/SOL/BTC: balances, fees, broadcast, status, validate) + `AdapterRegistry` gateway                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 3     | Portfolio Engine + Intelligence         | 🔄 In progress | `packages/portfolio` aggregation done (15 tests) + `packages/intelligence` FINANCIAL BRAIN done (38 tests: analytics/insights/alerts/scenario/tax + verified AI narration). Remaining: live BalanceSource/PriceSource/PositionSource wiring                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 4     | Intent Engine v1                        | ✅ Done        | `packages/intents` — 40 tests. NL→Intent (deterministic + schema-validated LLM) → ExecutionPlan; pure planner over injected sources; AI proposes, deterministic disposes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 5     | Execution Layer                         | ✅ Done        | `packages/execution` (12 tests, step machine) + `packages/providers` (16 tests — Aggregator Framework: pluggable swap/bridge/price/gas/sim providers, health scoring + circuit breaker + failover + quote aggregation + RouteOptimizer)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 6     | Risk & Security Engine                  | ✅ Done        | `packages/risk` — 20 tests. Immune system: threat intel (hard blocks) + heuristic detectors + probabilistic-OR scoring + configurable policy engine → allow/confirm/block. Standalone. Fills RiskProvider                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 7     | Backend Platform                        | 🔄 In progress | **Composition root landed** — `packages/runtime` (6 tests) wires the REAL IntentEngine + RiskEngine + identity ContactBook into one `createWalletRuntime()` → NL→verified plan end-to-end (real risk blocks sanctioned recipients). Prices/routes = deterministic stubs (swap-in real vendor plugins). NEXT: HTTP surface LIVE + HOSTED — services/api mounts POST /v1/intents/plan over the runtime (dev-seed holdings, gated to non-prod). Server runs on localhost:8080 via `npx tsx watch --tsconfig tsconfig.base.json services/api/src/main.ts` (.claude/launch.json). VERIFIED live: transfer→plan, swap→plan(route), over-balance→rejected, empty→400. ⚠️ pnpm -r build is BROKEN (cross-package tsc TS6059) + node dev script fails (resolves to dist); run from source via tsx+--tsconfig base ONLY. Real dist build = Phase 10. EXECUTION SEAM LIVE — runtime.execute(plan, {signer,gateway}) drives the ExecutionEngine (RuntimeStepDriver: simulate→device-sign→broadcast→confirm; park-never-strand on revert). Full NL→plan→sign→broadcast→confirm path exists (fake signer/gateway for tests; real = core signer + chains adapter). CHAINS WIRED — readChainHoldings/mergeHoldings (live native+token balances→Holding[]) + AdapterChainGateway (real broadcast+status over BlockchainAdapter, powers execute). Fake adapter in tests, live RPC in prod. **FULL LOOP OVER HTTP** (commit d314444): services/api now exposes POST /v1/intents/{plan,authorize,execute} + GET /v1/portfolio — authorize/execute act on a SERVER-issued plan (planId→plan cache, client can't forge one); /execute RE-runs Risk+Policy server-side and refuses unless mayProceedToSign; /execute registered ONLY when an executor seam is wired (prod ships none → route absent). `createDemoExecutor()` = fake signer + fake gateway + REAL bounded gas planner. api 14→19 tests. NEXT: real vendor plugins (price/DEX), real device signer + live chain gateway for prod /execute. |
| 8     | Client Apps (web + mobile)              | 🔄 In progress | **Web frontend LIVE** — `apps/web` (Vite+React+TS). Chat-style intent box → `POST /v1/intents/plan` (Vite proxy `/v1`→:8080, no CORS) → renders plan/clarify/rejected cards with risk badges, on the design tokens (indigo accent, light+dark). **FULL LOOP IN THE BROWSER** (commit d33e65f): the plan card now walks Confirm & authorize → AuthzView (gate + risk/policy + reasons + step-up requirements) → Sign & execute → ExecResultView (✓ completed / ⏸ parked-funds-safe / ✕ failed + funds location + steps); when the permission doesn't allow signing the execute button is withheld (UI can't skip the gate). VERIFIED in the browser via preview MCP: swap→Authorized(risk+policy)→✓ Executed, confirmed, funds on eip155:1; console clean; prod bundle builds. Run: `pnpm --filter @intent-wallet/web dev` on :5173 (`.claude/launch.json` has `api`+`web`). Thin client — no logic, renders the API. **AI-NATIVE REDESIGN** (commit 89b9791): reworked from search-box+table into a ChatGPT+Apple-Wallet experience — landing hero ("What would you like to do?"), chat bubbles + typing indicator, sticky rounded composer w/ example pills; portfolio CARDS (gradient Net-Worth + per-asset allocation cards); the **AI PLANNING STEPPER** (the differentiator) — a plan renders as an animated timeline Understood→Security→Best route→Cost→Authorize→Execute, each stage from real plan/permission/execution; **route graph** (from-asset→step(chain)→to-asset from real steps); one-line reasoning summary. **Consumes @intent-wallet/sdk** (dogfood; aliased to source). NO fake P/L/24h/savings (need historical price + router route-comparison sources — honestly deferred). Verified live: swap→authorized→✓executed all stages lit; stake $2k→"Needs step-up" gate withholds execute. **User product steer (2026-07-06):** wants ChatGPT+Stripe+Apple-Wallet UX (done ✓); P0 remaining = Universal Identity 3-address UI (BTC+SOL+EVM), real wallet connect (WalletConnect/hardware), tx simulation surfaced, tx history, multi-chain balance discovery — all need real device signer/RPC (external). Mobile Expo still deferred.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |     |
| 9     | Gas Abstraction & Smart Accounts        | ✅ Done (core) | `packages/gas` — 11 tests. Deterministic decision core: BOUNDED sponsorship (per-tx + per-user-per-day budget, fails→user_pays, can't drain paymaster), fee-token selection (pay gas in USDC/any, round-up+margin), capped EIP-1559 params (never overpay on a spike), UserOp batching. DECIDES; ERC-4337 UserOp build + paymaster sign = execution/infra. **WIRED INTO EXECUTION** (commit fa24381): `packages/runtime/src/gas.ts` — RuntimeGasPlanner drives the GasEngine per step (sponsorship→bounded EIP-1559→fee-token) via an injected FeeMarketSource; RuntimeStepDriver computes the gas plan BEFORE device-sign and threads it into StepSigner.sign(step, plan, gas?) (EVM-gated → null for non-EVM, native fees; non-custodial preserved). staticFeeMarketSource = offline source. runtime 14→21 tests. [Doc](docs/architecture/33-gas-abstraction.md), [ADR-0050](docs/adr/0050-gas-abstraction.md). Remaining: real paymaster/bundler + live fee-market source (RPC eth_feeHistory + price oracle) — needs external RPC.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |     |
| 10    | Production Hardening                    | 🔄 In progress | **PRODUCTION BUILD FIXED** — `pnpm -r build` now works (all 28 projects, cold, exit 0) + `node services/api/dist/main.js` serves the API + plan endpoint with ZERO dev tooling. Fix: build tsconfigs override `paths:{}` so deps resolve to their built `dist` (dev/test keep the src paths); topological order. tsx = the dev-only runner (`pnpm dev`). Remaining Phase-10: external security audit, perf cert, real vendor/RPC + device signing, secrets/infra.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 11    | Launch & Developer Platform             | ⬜ Not started |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

**Legend:** ⬜ not started · 🔄 in progress · ✅ done (tests green + docs)

## 📜 Design-Doc Track (user's prompt roadmap — runs alongside the Build phases)

The user pastes numbered "prompts" (#1, #2, #3, … up to ~#25). Several are **already satisfied by shipped code or existing docs** — do NOT rebuild; EXTEND the existing artifact and note it here. Map of prompt → status → artifact:

| Prompt topic                                                 | Status      | Artifact / why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Vision & PRD                                                 | ✅          | [requirements.md](requirements.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| System Architecture                                          | ✅          | [docs/architecture/](docs/architecture/README.md) — 11 docs, 12 diagrams                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **UI/UX & Design System**                                    | ✅          | [docs/design/](docs/design/README.md) — 10 docs, 9 diagrams, every screen                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Engineering Foundation / repo standards                      | ✅          | [docs/handbook/](docs/handbook/README.md) — 6 docs (standards, shared-lib contracts, envs/CI-CD, quality gates, milestones+DoD, team) + REAL enforcement: commit validator, CODEOWNERS, PR template, CI workflow, editorconfig/prettier, .env.example                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Tech Selection & ADR Lock                                    | ✅          | [docs/adr/](docs/adr/README.md) — **28 formal ADRs**, one per component (mobile/backend/db/cache/broker/RPC/crypto/AI/auth/push/analytics/observability/storage/cloud/k8s/CI-CD/secrets/IaC/testing/contracts/topology/automation), each with ≥3 alternatives + maintenance/scaling/security lenses. Scored comparisons in architecture/09                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Project Bootstrap                                            | ✅          | Real foundation CODE: `config`/`observability`/`events`/`ui` + `services/api` (Fastify) + lint/hooks/coverage + infra baseline. 149 tests. See [docs/handbook/06-bootstrap.md](docs/handbook/06-bootstrap.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Wallet Core                                                  | ✅ shipped  | `packages/core` — 126 tests. Identity + vault + FULL tx signing (EIP-1559/712/191, BTC PSBT, SOL) + WalletManager/SigningManager/SessionManager/SecureStore. [Threat model](docs/security/wallet-core-threat-model.md), [ADR-0029](docs/adr/0029-wallet-core-manager-and-signer-architecture.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Universal Identity                                           | ✅          | Engine done: core `HDKeyring` triple + `packages/identity` (model/address-mgmt/contacts) + `packages/portfolio` aggregation. [Engine doc](docs/architecture/11-universal-identity.md), [ADR-0030](docs/adr/0030-universal-identity-and-portfolio-layering.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Blockchain Adapter Layer                                     | ✅          | `packages/chains` — 69 tests. Full `BlockchainAdapter` (EVM/SOL/BTC) + `AdapterRegistry` gateway. [Doc](docs/architecture/12-blockchain-adapters.md), [ADR-0031](docs/adr/0031-blockchain-adapter-layer.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Intent Engine                                                | ✅          | `packages/intents` — 40 tests. Parser (deterministic→LLM), resolver, planner→ExecutionPlan, safety. [Doc](docs/architecture/13-intent-engine.md), [ADR-0032](docs/adr/0032-intent-engine-planner-and-plan-outcome.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Execution Engine                                             | ✅          | `packages/execution` — 12 tests. Persisted resumable step machine (simulate→broadcast→confirm→verify, park-not-strand). [Doc](docs/architecture/14-execution-engine.md), [ADR-0033](docs/adr/0033-execution-engine.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Provider/Aggregator Framework                                | ✅          | `packages/providers` — 16 tests. Pluggable swap/bridge/price/gas/sim providers + health scoring + circuit breaker + failover + quote aggregation. [Doc](docs/architecture/15-provider-framework.md), [ADR-0034](docs/adr/0034-provider-aggregator-framework.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Route Optimizer                                              | ✅          | `packages/router` — 17 tests. STANDALONE multi-factor optimizer (weighted scoring + bounded ML predictor). [Doc](docs/architecture/16-global-route-optimizer.md), [ADR-0035](docs/adr/0035-global-route-optimizer.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Risk & Security Engine                                       | ✅          | `packages/risk` — 20 tests. STANDALONE immune system (threat intel + detectors + probabilistic-OR scoring + policy). [Doc](docs/architecture/17-security-risk-engine.md), [ADR-0036](docs/adr/0036-security-risk-engine.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Portfolio Engine                                             | 🔄          | `packages/portfolio` aggregation done (15 tests); needs live BalanceSource/PriceSource wiring (Build Phase 3)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Portfolio Intelligence Engine                                | ✅          | `packages/intelligence` — 38 tests. STANDALONE financial brain: deterministic analytics (allocation/PnL/TWR/risk/health) + insight/alert/scenario(IL+β)/tax(FIFO/LIFO/HIFO/avg) engines + AI narration boundary that only cites verified numbers. [Doc](docs/architecture/18-portfolio-intelligence.md), [ADR-0037](docs/adr/0037-portfolio-intelligence-engine.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| AI Financial Copilot (decision layer)                        | ✅          | `packages/copilot` — 27 tests. Constrained orchestrator: LLM only picks tools + drafts prose; recommendations/gating/verify/confidence are code. No-execute by construction, fact-grounded, policy-gated. [Doc](docs/architecture/20-ai-copilot.md), [ADR-0039](docs/adr/0039-ai-financial-copilot.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Universal Policy Engine (authorization)                      | ✅          | `packages/policy` — 59 tests. Zero-trust deterministic gate; typed rules + conflict resolution + composeWithRisk (most-restrictive) + tamper-evident audit. Nothing reaches execution without it. [Doc](docs/architecture/19-policy-engine.md), [ADR-0038](docs/adr/0038-universal-policy-engine.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Automation & Workflow Engine                                 | ✅          | `packages/automation` — 24 tests. Autonomous OS: typed workflows (trigger→conditions→actions) + NL compiler + deterministic scheduler; EVERY run gated by Policy(+Risk); non-custodial session-key exec; kill-switch/pause/idempotency. [Doc](docs/architecture/21-automation-engine.md), [ADR-0040](docs/adr/0040-automation-workflow-engine.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Universal Settlement Engine ("Stripe of Web3")               | ✅          | `packages/settlement` — 7 tests. Mandatory front door to execution: deterministic/idempotent/resumable pipeline (preflight→liquidity→quote-lock→gas→execute→cross-chain→reconcile→portfolio→notify) + recovery classification + replayable ledger. MANDATORY pre-flight closes the review's CRITICAL#1 stale-plan gap. [Doc](docs/architecture/22-settlement-engine.md), [ADR-0041](docs/adr/0041-universal-settlement-engine.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Decentralized Solver Network                                 | ✅          | `packages/solver` — 9 tests. STANDALONE. Competitive execution (intents/UniswapX-style): staked solvers propose → platform VERIFIES (structural + independent SIMULATION, never trusts claims; over-claim→slash) → reputation-weighted select → settle. Staking/slashing anti-Sybil/spam; sealed anti-front-run. Solvers propose, signature disposes. [Doc](docs/architecture/23-solver-network.md), [ADR-0042](docs/adr/0042-decentralized-solver-network.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Observability, SRE & Self-Healing                            | ✅          | `packages/reliability` — 10 tests. STANDALONE SRE brain: SLI→error-budget burn→alerts(page/ticket, cooldown)→BOUNDED recovery. DECIDE-not-act (injected actuator); safety = freeze/security→escalate/rate-limit→escalate/no-loop. Telemetry pipeline = ADR-0018 infra. [Doc](docs/architecture/24-observability-sre.md), [ADR-0043](docs/adr/0043-reliability-and-self-healing.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Global Scalability Platform (100M+ users)                    | ✅          | `packages/scale` — 37 tests. STANDALONE. Deterministic decision core: bounded AUTOSCALER (multi-signal max-wins→desired replicas, clamp+rate-step+anti-flap+cooldown), resilience toolkit (token-bucket ratelimit, bulkhead=backpressure, generic breaker, retry w/ terminal+deadline guards, priority shed=degrade-not-brick, critical protected), region router (active-active weighted / active-passive failover, never routes to a dead region, shuffle-invariant), cache invalidation (dependency cascade, cycle-safe). DECIDE-not-act (injected ScaleActuator; a failed apply doesn't start the cooldown). Consistency chosen per-domain (strong=money/authz, eventual=reads). Infra (DNS/LB/K8s/mesh/DB shard/Kafka/multi-region/DR/cost/load-test) = doc+ADR. [Doc](docs/architecture/25-global-scalability.md), [ADR-0044](docs/adr/0044-global-scalability.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Compliance & Governance Platform (jurisdiction-configurable) | ✅          | `packages/compliance` — 37 tests. STANDALONE. Policy-driven, jurisdiction-configurable layer OUTSIDE core wallet logic — rules are VERSIONED DATA (JurisdictionProfile), engine generic, new region = new profile not code. Gateway = fail-closed + most-restrictive (allow/require_disclosure/require_consent/block) + obligations (report/screen/disclose/retain/audit). Versioned profile registry (monotonic, one active per id). Consent (lapsed=missing). Privacy: retention + DSAR erasure RECONCILED against legal holds (unparseable dates fail-safe=retain). Governance: RBAC deny-by-default + MAKER-CHECKER approvals (proposer≠approver) + emergency freeze + flags. Hash-chained tamper-evident audit (PII by-id, secure export proves full chain). Deterministic reporting. GATES + RECORDS, never signs/holds funds (non-custodial). Boundary: compliance PROVIDES jurisdiction config that risk/policy ENFORCE (no dup). [Doc](docs/architecture/26-compliance-governance.md), [ADR-0045](docs/adr/0045-compliance-and-governance.md)                                                                                                                                                                                                                                                                                                                                                                         |
| Plugin Marketplace & Extension SDK (ecosystem)               | ✅          | `packages/plugins` — 34 tests. STANDALONE security core. Third parties extend WITHOUT touching core; doctrine: plugin PROPOSES, code VERIFIES, device signature DISPOSES. CAPABILITY permissions where keys/signing/DB/security-internals are NOT in the vocabulary (FORBIDDEN_METHODS always denied even with all perms; deny-by-default; prototype-key safe). TRUST LEVELS (official/verified/community/experimental) = escalating permission CEILING (hard wall) + warnings + resource caps; user approval is a 2nd independent gate; official auto-approves. Signing GAUNTLET (integrity=codeHash + trust-claim + authorized-signer maxTrust + signature over canonical manifest + revocation), fail-closed. Semver (caret/tilde/comparators/prerelease) + dep resolution + caret host-API compat. Sandbox = resource limits (clamped to trust) + scoped method surface; the isolate runtime is infra. Lifecycle state machine + decide-not-act health auto-suspend (never auto-resume). Event-sub authz (no side-channel). Registry facade deep-copies manifest (no post-register escalation); auto-revoke suspends running + blocks resume. `intent.create`=PROPOSE only. ✅ multi-agent adversarial review RE-RUN (commit d29c410): 9 confirmed findings fixed — canonicalManifest now signs `entry`+`resources` (was: entry-swap supply-chain gap), authorizeEvent counts denied subs as violations (was: free event side-channel probing), Object.hasOwn event lookup (prototype-key safe), semver excludes prereleases from stable ranges + fails-closed on malformed ranges (was: 2.0.0-evil satisfied ^1.0.0 / bad range threw). +6 regression tests. [Doc](docs/architecture/27-plugin-marketplace.md), [ADR-0046](docs/adr/0046-plugin-marketplace.md) |
| Capability Registry (user idea: dynamic chain/provider capabilities) | ✅ | `packages/capabilities` — 26 tests. STANDALONE deterministic core. Versioned `ChainCapabilityProfile` (monotonic, single-active, structuredClone) keyed on **CAIP-2** ids + static health-free `ProviderRouteDeclaration`s + `CapabilityService` facade (supportsChain/supportsCapability/feeModelFor/canTransfer/canSwap/canBridge/checkStep/checkPlan), ALL fail-closed. Boundary: answers CAN-it from static DATA; live health stays in providers, scoring in router. WIRED into `WalletRuntime.plan()` — an infeasible plan (stake on a non-staking chain, bridge to an undeclared route) is rejected BEFORE authorize/sign; a new chain = a new profile, not code. Adversarial review (15/18 confirmed, fixed). [Doc](docs/architecture/34-capability-registry.md), [ADR-0051](docs/adr/0051-capability-registry.md) |
| Security hardening · Testing · DevOps · Monitoring           | 🟡 baseline | design in architecture docs 05/06; hardening = Build Phase 10                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| SDK/API · Growth · Whitepaper · GTM · Launch checklist       | ⬜          | Build Phase 11 + late design prompts                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

**Two tracks, one project.** Design Track = documents; Build Track = the 12 code phases (§ table above). When a pasted prompt maps to ✅/🔄, tell the user it's done/underway and point at the artifact rather than regenerating it. When it maps to a baseline doc, EXTEND that doc. Only truly-new topics (e.g. dedicated Whitepaper, GTM) get fresh docs. Never fork a second source of truth.

## ▶ NEXT ACTION

**✅ P1 UI BATCH (unlock-not-needed to CODE) — token-send picker + multi-account (2026-07-06). `2b76961` TOKEN-SEND PICKER: Send panel now has a per-chain asset row (ASSETS_FOR: sepolia ETH+USDC-erc20, devnet SOL+USDC-spl, testnet tBTC); picking re-reads balance (getErc20Balance; SPL shows "—" no reader yet) + drives amount/review; doSend routes erc20→sendErc20Transfer / spl→sendSplTransfer via new broadcast.decimalToBase; mainnet guard still runs. `a006ade` MULTI-ACCOUNT HD: wallet.ts tracks active index + count (2 ints in localStorage 'iw.accounts.v1'; seed never leaves vault); activeAccountIndex/accountCount/setActiveAccount/addAccount; EVERY read (currentIdentity/evmAddress/sol|btcPublicKey) + sign (SigningManager already took accountIndex) uses active index; create/import reset to 0, wipe clears. Web: account pills + "＋ Add account" in unlocked panel + a useWalletKey() hook (unlock+index) so LiveBalances/Insights refetch on switch. Both: web typecheck + prod build + clean-load 0 errors; VISUAL pending unlock. Remaining P1 verifiable-without-unlock UI: address book+ENS, revoke-allowance screen, manual swap screen, stuck-tx recovery. Vendor-gated: on-chain history + token balances (indexer), real DEX RouteProvider. — LLM block below.**

**✅ REAL ANTHROPIC LLM PARSE PATH LIVE (2026-07-06, commit 0d78296) — the "AI-native" headline is finally real. User pasted an Anthropic API key in-chat → it lives ONLY in gitignored services/api/.env (IW_LLM_API_KEY; main.ts does a guarded process.loadEnvFile()); git-grep verified absent from the tracked tree before commit; RECOMMEND THE USER ROTATE IT (pasted in plaintext chat). services/api/src/llm.ts = fetch-based Messages API LlmClient: ONE forced tool `emit_intent` (tool_choice type:tool) whose input_schema mirrors IntentSchema — NOTE Anthropic requires top-level type:"object" in input_schema, so the intent UNION is wrapped in a required `intent` field (root anyOf → HTTP 400 "input_schema.type: Field required"); ALSO `temperature` is DEPRECATED on Claude 5 models (HTTP 400 "temperature is deprecated for this model") — don't send it; determinism cage = forced tool + downstream Zod, not sampling params. Utterance travels as a USER message only (never system); context hints (heldSymbols/contactNames — no addresses/keys) go in system. Errors throw → CompositeParser degrades to clarify (never guesses). Wired via main.ts gated on IW_FLAG_INTENTS_LLM_PATH (its FIRST real consumer) && IW_LLM_API_KEY, into BOTH the dev runtime and makeUserRuntimeProvider (llm dep). 4 unit tests w/ fake fetch (injection posture: hostile utterance in user turn only, 1 forced tool, bounded max_tokens 500; schema-invalid output → clarify). VERIFIED LIVE (claude-sonnet-5, model from IW_LLM_MODEL_PARSE): "could you please move a hundredth of an ether over to 0x1111…" → REAL plan Send 0.01 ETH (10^16 wei exact — the model converted "a hundredth"→"0.01"); HINGLISH "bhai 0x2222… ko point zero one eth bhej do" → Send 0.01 ETH plan. Parse→resolve→plan→risk end to end off natural language. api 69 tests. Debug workflow that found both API quirks: run the client directly via `node --env-file=.env --import tsx <script>` from services/api and print the raw error body. NEXT: the web chat now speaks natural language through the same endpoint (visual pending unlock); remaining P1 = UI wallet screens (unlock-gated) + indexer/DEX vendor items.**

**✅ P1 STARTED — backend batch done (2026-07-06; user: "start p1"/"chalu rakh"). PLUS `d970cc4` INTELLIGENCE ENGINE SURFACED — GET /v1/portfolio/insights (session-guarded): services/api/src/insights.ts folds Holding[]+prices → PortfolioSnapshot (one `token` position per (asset,chain) in µUSD; unpriced asset = 0 value + stale flag, engine propagates it — no fake data; toJsonSafe bigint→decimal-string) and runs PortfolioIntelligenceEngine.analyze over the SAME holdingsFor/pricesFor seam instances the per-user runtime uses (main.ts builds ONE pair per process, shared price cache; local=dev seed, deployed=live RPC+CoinGecko). Verified: net worth 11e9 µUSD for 3 ETH@$2000+$5k USDC, positionCount 4, healthScore, honest stale bit; 401 unauth. api 65 tests. THEN `499345f` INSIGHTS VIEW SHIPPED — SDK gained PortfolioInsights wire type (mirrors intelligence EXACTLY: byAsset/bySector/byChain, hhi/effectivePositions, diversification/healthScore, insight code/severity/detail) + getInsights()/client.insights()/safe (sdk 15 tests, dist rebuilt); web api.ts getInsights + InsightsPanel in the unlocked dashboard (health, net worth, diversification, stable buffer, allocation bars, severity-tagged insights, honest stale badge, "analyzes only never signs" footer). VERIFIED LIVE after api restart: /v1/portfolio/insights returns netWorth $16,500 (exact dev-seed math), healthScore 93.28, diversification 81.18, stable 30.3%. Web clean-load 0 errors; panel VISUAL pass pending wallet unlock. Rest of the P1 batch below: P0 status given honestly: buildable-here P0 all done; remainder is vendor/legal/infra (sanctions feed, geo/ToS/KYC, keyed RPC) + mainnet-spend UX (needs user's real-funds go) + ExecutionEngine-routing (on-chain amountOutMin already covers the swap invariant). Then ran the P1 loop backend-first (fully verifiable, no wallet-unlock friction): `057138e` JWT SESSION REVOCATION — tokens carry a jti; SessionRevoker (InMemory+Redis, async/shared-across-replicas) = per-jti blocklist + per-subject monotonic not-before cutoff; ONE revoker instance backs both auth routes + the intent guard; POST /v1/me/logout (this session) + /v1/me/logout-all; web signOut revokes server-side + a "everywhere" control. `fc9eb09` PROMETHEUS /metrics (prom-client RED: http_requests_total + duration histogram, labelled by method/ROUTE-PATTERN/status — pattern not URL so cardinality can't explode; + node process metrics; own registry per app) + W3C traceparent propagation threaded into the request logger (full OTel-export deferred — needs a collector). `8fd72d0` OPENAPI enriched — real per-route request/response schemas + ProblemDetails + Bearer securityScheme (protected routes marked); still onRoute-introspected so it can't claim an unmounted endpoint. `4960ab7` E2E over the PROD-WIRED path (auth-required + per-user runtime + pg-mem Postgres + ioredis-mock Redis + revoker, together): unauth→401, SIWE→plan→authorize (Postgres round-trip, UUID plan id), /execute absent (on-device), logout kills one session not another, + an SDK↔API CONTRACT test (typed @intent-wallet/sdk client over a Fastify-inject transport, Bearer via authToken). api 61 tests. GOTCHA (cost me a debug): the api resolves @intent-wallet/sdk via its BUILT DIST (exports→dist/index.js), so a stale dist (pre-authToken) makes the SDK drop the auth header → 401; CI's `pnpm -r build` gate rebuilds it, locally run `pnpm --filter @intent-wallet/sdk build` before the e2e. Deps added: prom-client, pg+pg-mem, ioredis+ioredis-mock, @types/*. NEXT P1: verifiable-logic-but-UI-visual-needs-unlock (stuck-tx recovery, token-send picker, multi-account switch, address book+ENS, revoke-allowance, manual swap, surface Intelligence engine) OR infra/vendor (real Anthropic LLM+injection gate, on-chain history+token balances via indexer, real RouteProvider+DEX vendor adapters). — infra + review block below.**

**✅ INFRA WIRED + ADVERSARIALLY REVIEWED — Postgres/Redis/per-user-runtime/readiness, then 10 bugs caught & fixed (2026-07-06; user: "kaho toh main woh likh deta hoon" = author it). This took the deployed backend from a disabled stub to a real multi-tenant surface, using in-process test doubles (pg-mem, ioredis-mock) so the LOGIC is verified here, not just typechecked. Commits: `f150fa3` PostgresPlanStore (drop-in PlanStore over IW_DB_URL; plans(plan_id PK, plan JSONB, owner, created_at); parameterized SQL; ON CONFLICT upsert; verified vs pg-mem — a REAL in-process Postgres). `cb78163` Redis: NonceStore is now an async interface (InMemoryNonceStore + RedisNonceStore using PX TTL + ATOMIC GETDEL for one-time-use across replicas) + @fastify/rate-limit pointed at the same Redis (shared limit); verified vs ioredis-mock. `adba928` PER-USER RUNTIME (the XL blocker): services/api/src/runtime-provider.ts — RuntimeProvider seam; makeUserRuntimeProvider discovers holdings + prices FRESH per request (no cache = no stale/fake balance) → createWalletRuntime; makeLiveHoldingsSource (discoverHoldings over real RPC; SIWE subject IS the EVM addr) + makeCoinGeckoPrices (public feed). intents.ts routes now resolve the runtime per-request from request.auth.subject (fixed runtime OR resolver — a function vs object switch). main.ts deployed path mounts it + Postgres; local keeps the dev seed. Verified: Alice(5 ETH) gets a plan, Bob(0) doesn't. `7c7fb8f` real /readyz probes (plan-store query + redis.ping). THEN ran an ADVERSARIAL REVIEW WORKFLOW (wf: 14 agents, 4 dims × verify-each-finding) → 10 CONFIRMED defects, all fixed in `3432744`: [CRIT] per-user runtimes each used the per-instance ordinal id gen → every user's 1st plan = 'plan-1' → shared-store PK-upsert let one tenant OVERWRITE another's plan → fix: inject UUID ids in makeUserRuntimeProvider. [CRIT] deployed could boot with IW_REQUIRE_AUTH unset (default false) → routes open + everyone = 'demo-user' → cross-tenant authorize/execute → fix: config superRefine now REQUIRES IW_REQUIRE_AUTH=true non-local (fail-closed). [HIGH] GET /v1/portfolio had no guard (add `guarded`); makeLiveHoldingsSource fed literal principal to eth_getBalance (validate 0x-addr, reject else); pg Pool leaked on shutdown (planStore.close() in close()); ioredis had no 'error' listener → crash on transient drop (attach it). [MED] prune had no tiebreaker (ORDER BY created_at DESC, plan_id DESC = total order); SqlitePlanStore threw on a legacy .plans.db w/o owner col (ALTER-add migration); per-request CoinGecko fetch → self-rate-limit (short-TTL cache + in-flight dedup, still hard-fails not stale). config 8 / api 52 green. LESSON REINFORCED: adversarial review of new money-adjacent infra pays — it found 2 criticals typecheck+unit-tests missed. NOTE: prod RPC/price are still PUBLIC-tier (CoinGecko + publicnode) — swap for keyed before real load; and /execute stays absent in prod (signing is on-device). STILL REMAINING (infra/vendor/legal, can't do here): real ThreatIntel/sanctions (screening false for everyone), geo-block+ToS/Privacy+KYC, real Anthropic LLM (none wired), a real on-device Executor for server-driven automation, keyed RPC/price, a live cluster to run the CD + exercise it. — the P0 rest-in-loop is below.**

**✅ P0 "REST-IN-LOOP" — every launch-blocker that's verifiable WITHOUT external infra is now done (2026-07-06; user: "start rest of p0 in loop", after the roadmap workflow). This followed the multi-agent roadmap (wf: 11 agents) whose honest verdict: "a cryptographically real, non-custodial TESTNET wallet trapped behind a disabled prod backend; a deployed preview shipped fake balances as if real." Closed, each committed + tested: ① `f571c99` GATE DEV-SEED/DEMO/IDENTITY ON isLocal not isProductionLike — a deployed `preview` was mounting buildDevRuntime()'s hardcoded balances + the 'signing simulated' demo executor on a shared URL (the #1 doctrine hole). Added config `isLocal(cfg)`; main.ts gates runtime/executor/identity on it; insecure JWT fallback is now local-only (throws otherwise). ALSO fixed a LATENT bug: the SIWE commit added IW_AUTH_SECRET to the required-non-local list but never updated the PROD_MIN test fixture, and pre-commit runs typecheck+lint but NOT tests → config tests were RED on main (now green + an isLocal test). ② `d7bc728` EDGE HARDENING — @fastify/cors (IW_CORS_ORIGINS allowlist, empty→same-origin; the browser couldn't reach ANY auth endpoint cross-origin before), @fastify/rate-limit (IW_RATE_LIMIT_MAX global + tighter per-route on /v1/auth/*; NOTE the plugin THROWS errorResponseBuilder's return, so return an AppError (tooManyRequests) to route through the app error handler → 429 problem+json), @fastify/helmet (CSP off, it's a JSON API). Added tooManyRequests(429) to observability. 5 hardening tests. ③ `790ed1e` SWAP FUND-SAFETY — user-controlled slippage (0.1/0.5/1%) replacing the hardcoded 0.5% amountOutMin + minReceived shown before signing; settlement-safe sequencing in broadcast.sendSwap (read allowance→approve ONLY if short→WAIT for the approve receipt (revert throws)→eth_call PREFLIGHT the swap→broadcast) so the swap can't be mined before the approval and revert while the UI says done. ④ `6467bdc` PRINCIPAL BINDING + PLAN OWNERSHIP — PlanStore now stores {plan, owner} (SqlitePlanStore owner column); intents routes bind principal to request.auth.subject (never the client's claim; falls back to dev principal only when auth off) and require a matching owner on authorize/execute (foreign/absent plan → identical 'no such plan' so ownership can't be probed). Test: user B gets 404 on user A's plan. ⑤ KAT conformance was ALREADY covered in packages/core/test/keyring.test.ts (EVM 0x9858…EcaEda94 MetaMask, BTC bc1qcr8te4… BIP-84, SOL vs ed25519-hd-key SLIP-0010 reference) — the roadmap surveyor missed it; verified green, CI-gated. ⑥ `f187094` SELF-CUSTODY UX — Receive modal per chain (in-browser QR via `qrcode` dep, no external service; BTC shows the tb1q… testnet addr) + tx PREVIEW gate (Review→ shows amount/to/network/fee → Confirm & sign) on the manual Send + create-flow SEED-VERIFY quiz (re-enter 2 random words) + typed 'REMOVE' wipe confirmation. Also added `pnpm -r build` as a CI gate (ci.yml) since it was missing (a build-only break would've only shown in the docker image). VERIFY GOTCHAS reused: pre-commit does NOT run tests (only typecheck+lint) — run `pnpm --filter <pkg> test` explicitly; the preview console buffer holds STALE HMR error artifacts (timestamped t=…) — to check real health install a console.error listener on the already-loaded page and watch 4s (0 = healthy), don't trust the buffer. STILL PENDING: #94 readiness probes = DEFERRED (nothing real to probe until Postgres/Redis land — an always-ok probe would be vacuous/doctrine-violating). The 3 client UIs (Receive/tx-preview/seed-wipe) are typecheck+prod-build+clean-load verified but their UNLOCKED-view visual pass is pending a wallet unlock (user chose "I'll unlock" but then said keep-going; won't wipe the existing 0xE754… testnet wallet to force it). REMAINING P0 is now INFRA-DEPENDENT ONLY (can't complete here): per-user prod WalletRuntime + real holdings source (keyed RPC), PostgresPlanStore (IW_DB_URL unread), Redis nonce+limiter (IW_REDIS_URL unread), real ThreatIntel/sanctions vendor (screening returns false for everyone today), geo-block + ToS/Privacy + KYC (legal), managed keyed RPC/price. Then P1: real Anthropic LLM (none wired — IW_FLAG_INTENTS_LLM_PATH has zero consumers), stuck-tx recovery, on-chain history, token balances, multi-account, address book. Do NOT commit the Helius key. — the DO-1,2,3 loop is below.**

**✅ THREE P0s CLOSED IN ONE LOOP — mainnet guardrails + CD pipeline + client-auth finish (2026-07-06; user said "DO 1,2,3"). ① MAINNET GUARDRAILS (commit ab865f8): packages/chains/src/guard.ts — a PURE, fail-closed gate between a signed tx and the wire (holds no keys, can only REFUSE). Three defenses: EIP-55 recipient checksum (a mixed-case typo is rejected; all-lower/upper accepted — implemented locally with the existing @noble/hashes so chains gains NO core dep), a mainnet acknowledgment gate (real-funds broadcast blocked unless acknowledgeMainnet), and a $1,000 mainnet spend cap (acknowledgeHighValue above it). `guardBroadcast`/`assertBroadcastAllowed`/`checkEvmRecipient`/`MAINNET_SPEND_CAP_USD` exported; ChainErrorCode gains GUARD_BLOCKED. Wired into EVERY apps/web/broadcast send path (native EVM/SOL/BTC, ERC-20, SPL, swap) via a `GuardAck` opt (testnet-only app → mainnet gate is future-proofing that fires the instant a mainnet chain is used; checksum protects today). 13 guard tests, chains 101. ② CD/DEPLOY (commit 5644640): Dockerfile+k8s already existed but nothing built/shipped the image. Added .github/workflows/release.yml (on main/v* → build gate `pnpm -r build` → docker build from infra/docker/api.Dockerfile → push GHCR sha/semver/latest → keyless cosign sign by digest → deploy-staging job `kubectl set image` to the signed digest, GATED on vars.DEPLOY_STAGING + a `staging` Environment so it's SKIPPED not faked until a cluster+KUBE_CONFIG exist) + infra/k8s/base/kustomization.yaml. Also added `pnpm -r build` as a CI gate (ci.yml) — it caught a REAL stale-.d.ts bug: build reads config's emitted d.ts (missing IW_AUTH_SECRET) while typecheck reads source, so CI's typecheck-only passed but a from-clean build failed; `pnpm -r build` (topological) fixes+guards it. VERIFIED here (no docker daemon): `pnpm -r build` exit 0; the image's `pnpm --filter @intent-wallet/api deploy --prod` prune step emits dist/main.js + bundles workspace deps; /healthz+/readyz (probed) exist; all YAML prettier-parses. ③ CLIENT AUTH FINISH (commit 186ea35): SDK ClientOpts gains `authToken` (string | resolver read FRESH per request) → attaches `Authorization: Bearer <token>` (3 SDK tests, sdk 15). apps/web/api.ts passes `() => currentSession()?.token`; a SessionBar in the unlocked wallet panel does sign-in/out + shows the address. Server: config IW_REQUIRE_AUTH (booleanish, default false) → intent routes (plan/authorize/execute) gated by makeAuthGuard(makeJwtVerifier) as a preHandler, threaded app.ts→v1 index→intents.ts; OFF on localhost, deployments flip it on (3 API tests: 401-without/200-with/open-when-off; api 32). VERIFIED LIVE (non-destructive, against the running app + the persisted SIWE session 0xE754…F960): the SDK stamps `Authorization: Bearer eyJhbGc…` (real 196-char JWT) onto BOTH POST /v1/intents/plan and GET /v1/status; locked UI renders clean, 0 active console errors. .env.example synced (commit 7cfb0ae: IW_AUTH_SECRET+IW_REQUIRE_AUTH). SessionBar's on-screen render is behind the wallet password (not re-driven; won't wipe the existing wallet) but its signIn() loop was verified live earlier this session + is unit-covered. The audit's original P0 board (tokens, DEX, DB, auth, guardrails, deploy, UI-gating) is now all GREEN — remaining is real-infra work only (a cluster to run the CD, funded mainnet to exercise the guards, Postgres for multi-replica). NOTE: the api process at :8080 predates these server changes (requireAuth defaults off so unaffected); restart it to pick up IW_REQUIRE_AUTH. — prior blocks below.**

**✅ REAL SIWE AUTH (2026-07-06, commit 1f59e0e) — audit auth P0 done. Replaced the reject-everything stub with EIP-4361 sign-in: wallet signs a server challenge IN-BROWSER (personal_sign, no key leaves), server recovers the signer (core recoverEvmAddress+hashPersonalMessage, v-normalized 27/28→0/1) + checks a one-time NonceStore + issues an HS256 JWT (node:crypto, zero deps). services/api/src/auth/{siwe,jwt}.ts + routes/v1/auth.ts (POST /v1/auth/{nonce,verify} + PROTECTED GET /v1/me via makeJwtVerifier replacing rejectingVerifier). Wired v1 index+buildApp+main.ts; config IW_AUTH_SECRET (required non-local, dev default). apps/web: wallet.signPersonalMessage+evmAddress, auth.ts signIn()/currentSession/signOut. VERIFIED: 6 api tests (full HTTP round-trip: 401 no-session, nonce→sign→verify→/v1/me 200, replay→401) + LIVE browser (real wallet signed in as 0xE754…F960, /v1/me 200, no-token 401). api 29 tests. REMAINING finish: a "Sign in" button in the UI + attach the token to SDK requests to gate them. NEXT P0s: mainnet guardrails (code-level: mainnet-chain warning + spend caps + checksum confirm at the broadcast layer — verifiable) then CD/deploy (Dockerfile + GH Actions build→push→deploy — can AUTHOR but can't run/verify here: no docker/registry/cluster). Also UI login-gating shipped (commit 10eed2a): locked=create/unlock only; unlocked=dashboard+chat+swaps. — all-in-loop detail below.**

**✅ "ALL IN LOOP" COMPLETE — SPL + real DEX + DB persistence all delivered (2026-07-06). #2 REAL UNISWAP v3 SWAPS (commit 2316d12): "swap 100 USDC for ETH" no longer simulated — chains/evm/uniswap.ts encodes QuoterV2.quoteExactInputSingle (byte-exact vs the on-chain-proven calldata), SwapRouter02.exactInputSingle, ERC-20 approve + Sepolia addrs (QuoterV2 0xEd1f6473…, Router02 0x3bFA4769…); 4 tests (chains 88). web/broadcast quoteSwap (QuoterV2 eth_call, best of fee tiers) + sendSwap (approve→exactInputSingle, both signed in-browser) + isSwappablePair. App PlanFlow detects a swap, fetches the LIVE quote (replaces stub youReceiveMin), executes real swap w/ 0.5% min-out. VERIFIED LIVE: Sepolia WETH/USDC pool IS liquid — "swap 100 USDC for ETH" quoted 0.006924 ETH (real ~14,311 USDC/ETH), execute signed approve+swap in-browser → node returned "insufficient funds for gas" (unfunded). A FUNDED wallet would confirm. #3 DB PERSISTENCE (commit 4f721ca): services/api/src/persistence/plan-store.ts — async PlanStore {remember,get,close}; InMemoryPlanStore + SqlitePlanStore (node:sqlite, file-backed, survives restart). Wired through intents route (Map→store, awaited) + v1 index + buildApp + main.ts (dev defaults to .plans.db, IW_PLAN_DB_PATH override). 3 tests (api 23). VERIFIED END-TO-END: issued plan-1 → RESTARTED api → POST /authorize {plan-1} → HTTP 200 allow (was 404 pre-change). Same schema/SQL = drop-in Postgres for multi-replica prod. Note: id counter resets on restart (plan-N collisions) — prod should use UUIDs. #1 SPL DONE (core, commit b9d0428): packages/chains/src/solana/spl.ts — `findAssociatedTokenAddress` (PDA via @noble/curves off-curve bumps, now a chains DEP) VERIFIED BYTE-EXACT against the official @solana/spl-token getAssociatedTokenAddressSync (2 real (owner,USDC) vectors: 5rCf1DM8…→Cs2C1WHg…, 9WzDXwBb…→FGETo8T8…); `compileMessage` (proper legacy-message account dedupe+privilege-order, payer idx 0); `buildSplTransferMessage` (createIdempotent ATA + transferChecked instr 12 = [12,u64le amount,decimals]). 5 tests (chains 84). apps/web/broadcast `sendSplTransfer` reuses the verified in-browser SOL sign+broadcast path over a REAL devnet USDC mint Gh9ZwEmdLJ8DscKNTkTqPbNwLNNBjuSzaG9Vp2KGtKJr (6dec, confirmed exists). SPL REMAINING: send-panel token selector on Solana tab + a confirmed devnet SPL tx (needs a devnet SPL token funded in). NOTE: the intents planner treats USDC as EVM-only (assetEcosystem), so Solana-USDC via the CHAT intent needs a multi-chain-asset change; the send-panel path avoids that. #2 REAL DEX (next): Sepolia Uniswap v3 QuoterV2 gives REAL quotes via eth_call, but testnet pools have ~no liquidity so swaps won't confirm — will wire real quotes + real swap-tx construction, honest that execution needs liquidity. #3 DB PERSISTENCE (after): wire Postgres (docker-compose has one, nothing connects) + a repo to replace the in-memory plan Map; needs a running Postgres to verify. Verification gotcha reused: preview_fill doesn't sync React state — use native value setter + dispatch('input'). Do NOT commit the Helius key. — ERC-20 + bridge detail below.**

**✅ REAL ERC-20 TOKEN TRANSFERS via the AI loop (2026-07-06, commit 5a4c250): "send 5 USDC to 0x…" typed in chat is now a REAL in-browser-signed ERC-20 transfer (was demo/absent — the audit's #3 P0). chains/abi: `encodeErc20Transfer(to,amount)` (0xa9059cbb + address word + uint256 word) + `encodeUint256` (overflow/neg guards), 2 byte-exact tests (chains 80). web/broadcast: Sepolia token registry (Circle testnet USDC 0x1c7D4B196…379C7238, 6dec) + `getErc20Balance` (adapter balanceOf reader) + `sendErc20Transfer` (encode → estimateGas w/ fallback-on-revert → sign EIP-1559 CONTRACT CALL {to:token,value:0,data} in-browser → broadcast). `isExecutableAsset`/`balanceForAsset`/`executeTransferStep` route known tokens to the ERC-20 path; App executableTransfer maps tokens→Sepolia. VERIFIED LIVE: USDC intent → planned → authorized → signed in-browser → Sepolia node returned "insufficient funds for gas...balance 0" (wallet unfunded) — proving the ERC-20 contract-call path reaches the real chain. To CONFIRM a USDC transfer: faucet Sepolia ETH (gas) + Sepolia USDC. NEXT: SPL (Solana token) transfers need ATA derivation + create-if-missing (bigger); then real DEX for swaps, DB persistence, SIWE auth, mainnet guardrails. — bridge + confirmed-tx detail below.**

**✅ WALLET ↔ AI LOOP BRIDGED — the headline feature is REAL now (2026-07-06, commit b8d5081): a natural-language transfer typed in the CHAT runs the real non-custodial loop end-to-end, NOT the fake executor. Prior state (per the honest audit): "AI executes your intent" was theater (fakeStepSigner + fakeChainGateway + hardcoded holdings). Now: server still PLANS (real deterministic parse + recipient resolution + Risk/Policy authorize — all real), and when the plan is a native transfer (ETH/SOL/BTC) AND the browser wallet is unlocked, the BROWSER signs in-browser + broadcasts to the live testnet (device disposes; runtime never signs). HOW: (1) `serializeOutcome` already put `steps[].params={asset,amountBase,to}` on the wire — added `params?` to SDK `PlanStep` type (packages/sdk/src/types.ts; web re-exports it). (2) services/api/src/main.ts dev holdings SEEDED with SOL (10) + BTC (0.1) so native-transfer intents plan instead of "you don't hold any SOL" (the browser enforces the REAL testnet balance). (3) apps/web/src/broadcast.ts: `executeTransferStep({asset,amountBase,to})` maps asset→testnet send (baseToDecimal), + `balanceForAsset` + `isExecutableAsset`. (4) apps/web/src/App.tsx PlanFlow: `executableTransfer(plan)` detects a native transfer; when unlocked, shows the REAL balance and the "Sign on device & execute" button now calls executeTransferStep (real, in-browser) instead of executeIntent (server/demo); honest footer "real signing in your browser (non-custodial)". Swap/stake + locked + token(USDC) cases keep the LABELED demo path (executeIntent). VERIFIED LIVE: typed "send 0.01 SOL to So111…112" in chat → planned → authorized (risk+policy) → signed in-browser → broadcast → CONFIRMED devnet (sig 5SmA7mbvDf9YbBHA4WZ6QpLvmN8Mv9RiZm3JxYZiQxKe5i5YFyu6cJJ2UcHnLNxBbhHu4m4GSiMon52G8R6rxLhA; Helius parsed it as a 0.01 SOL transfer). NEXT big pieces from the audit (P0): ERC-20/SPL TOKEN transfers (can't send USDC yet), real DEX quote/price feed for SWAPS (router+providers engines built-not-wired), server persistence (DB — in-memory Map dies on restart + breaks across replicas), real auth (SIWE — stub rejects all), mainnet guardrails. Note: gotcha — preview_fill doesn't sync React controlled-input state; drive React inputs via the native value setter + dispatch('input'). Do NOT commit the Helius API key. — confirmed-tx + ecosystem detail below.**

**✅ FIRST REAL CONFIRMED ON-CHAIN TX LANDED (2026-07-06, Solana devnet): the non-custodial browser wallet signed IN-BROWSER and broadcast a REAL, FUNDED, CONFIRMED SystemProgram transfer. sig `5KT4SBkFXihmHYBLCu5Ha3ZdUzHp5AjzN8NMr6KUPYYMkvwVPsBGu1N1QnUwdmMNc8icNb9EcNnkW2BjUnMd2sQZ` (explorer.solana.com/tx/…?cluster=devnet) — status SUCCESS, fee 5000 lamports, accounts [sender A8Nc8znKF…, recipient So111…112 wSOL-mint, SystemProgram], sender −0.100005 SOL / recipient +0.1 SOL (exact value conservation). Wallet SOL address was funded with 5 devnet SOL (faucet); broadcast via a user-supplied Helius devnet RPC (reliable sendTransaction; the public faucet + public airdrop were rate-limited/dry today — Helius faucet caps 1 SOL/project/day). VERIFIED independently on the PUBLIC RPC (getSignatureStatuses confirmed/finalized + getTransaction meta + balance delta). This upgrades SOL from "reached the chain (AccountNotFound)" to a genuine confirmed transfer — the whole doctrine (serializer builds → device signs → chain confirms, AI never signs) proven end-to-end with real value movement. NOTE: do NOT commit the user's Helius API key anywhere; it was used only for the airdrop-check + broadcast RPC, never persisted. Sepolia + BTC testnet remain "built + reaches chain, needs faucet funding to confirm" (captcha-gated faucets). — full ecosystem detail below.**

**"SABKA KRO" COMPLETE — ALL THREE ECOSYSTEMS BROADCAST FROM THE BROWSER (2026-07-06, commit ce0cecc, supersedes everything below): EVM(Sepolia) + SOL(devnet) + BTC(testnet) all sign in-browser (non-custodial) and broadcast to real testnets. BITCOIN added now: NEW `packages/chains/src/bitcoin/transaction.ts` — `buildBtcTransfer` builds an UNSIGNED native-SegWit (P2WPKH) PSBT via @scure/btc-signer (selectUTXO coin-selection + fee at sat/vByte + change), `p2wpkhAddressFor` derives the bech32 address (witness program is network-agnostic → the wallet's BTC pubkey encodes bc1q…/tb1q… by HRP only; the tb1q address shares the same `ks7jh…` witness program as the mainnet bc1q). 4 tests (chains now 78): address encoding, value conservation, full sign→finalize→extract round-trip, insufficient-funds guard. Added `INSUFFICIENT_FUNDS` to ChainErrorCode. FIXED the same browser fetch bug in `HttpRestTransport` (bind globalThis.fetch — was the "Illegal invocation" class fixed earlier for HttpJsonRpcTransport). `apps/web/src/wallet.ts` adds `btcPublicKey` + `signBitcoinPsbt` (core signBitcoinPsbt('btc',0,{finalize:true}) — key never leaves). `apps/web/src/broadcast.ts` adds `sendBtcTransfer` (live getUtxos + estimateFees from esplora → buildBtcTransfer → signBitcoinPsbt → BitcoinAdapter.broadcastRawTransaction POST /tx) + `getBtcTestnetBalance` + `btcTestnetAddress` + `parseSats`, DEFAULT_BTC_TESTNET_REST=https://blockstream.info/testnet/api. Send panel gained a 3rd tab "Bitcoin · testnet" + a sat/vByte fee field (wl-send-grid). VERIFIED LIVE against Blockstream testnet esplora: balance + UTXO + fee-estimate GETs all 200 for the wallet's tb1q… address; unfunded → honest "no spendable UTXOs" (UTXO model: can't build a 0-input tx, so unlike EVM/SOL there's no node-side broadcast error until funded — the build+sign+broadcast path is proven by the offline round-trip tests). ONE OP GOTCHA: adding @scure/btc-signer to chains means the tsx-run services/api must be RESTARTED after install (running process won't see the new symlink → ERR_MODULE_NOT_FOUND on /v1/* until restart; deployed builds resolve fine since chains declares the dep). Non-custodial doctrine intact across all 3. REMAINING to land CONFIRMED txs: just fund each testnet address from a faucet (Sepolia ETH / devnet SOL / testnet tBTC). — SOL/Sepolia/wallet detail below.**

**REAL SOLANA DEVNET BROADCAST WIRED (2026-07-06, commit 145ea68, supersedes the Sepolia block below for "sabka kro" progress): the browser wallet now broadcasts native SOL on devnet too. NEW `packages/chains/src/solana/transaction.ts` (`buildSolTransferMessage` + `assembleSolTransaction` + `encodeShortVec` + `SYSTEM_PROGRAM_ID`, exported from chains index; 5 byte-exact tests, chains now 74) compiles a legacy SystemProgram transfer message purely (short-vec prefixes, [1,0,1] header, DEDUPLICATED privilege-ordered account keys so a self-transfer from==to collapses to one key instead of AccountLoadedTwice, transfer discriminator [2,0,0,0]+u64-LE lamports) and frames [sigCount][sig64][message] as base64. `apps/web/src/wallet.ts` adds `signSolanaMessage` (ed25519 via core SigningManager, in-browser) + `solPublicKey` (hexToBytes of core account sol.publicKey). `apps/web/src/broadcast.ts` adds `sendSolTransfer` (pool.request getLatestBlockhash{finalized}→.value.blockhash → compile → sign → SolanaAdapter.broadcastRawTransaction) + `getSolTestnetBalance` + `parseLamports` (9-dec), DEFAULT_DEVNET_RPC=https://api.devnet.solana.com. WalletPanel send sub-panel gained a CHAIN TOGGLE (Ethereum·Sepolia | Solana·devnet) that swaps rpc/recipient-default/symbol/explorer (.wl-chain-tabs CSS). VERIFIED LIVE on real devnet cluster: distinct recipient AND self-send both return the node's own `AccountNotFound` "Attempt to debit an account but found no record of a prior credit" — the canonical unfunded-but-VALID-and-SIGNED proof (message deserialized + ed25519 sig verified on-chain). Key never leaves the browser. So EVM(Sepolia)+SOL(devnet) broadcast = DONE. NEXT for "sabka kro": BTC testnet (getUtxos → build+sign PSBT via @scure/btc-signer + core signPsbt → broadcast via BitcoinAdapter/HttpRestTransport) — the last per-ecosystem tx-construction piece; needs a funded testnet account to land a confirmed tx. — earlier Sepolia/wallet notes below.**

**REAL SEPOLIA BROADCAST WIRED (2026-07-06, commits 6756d93 fix + 4e06512): `apps/web/src/broadcast.ts` consumes `@intent-wallet/chains` (aliased to source; browser-safe) → the non-custodial browser wallet BROADCASTS to real Sepolia end-to-end: live nonce (getNonce) + EIP-1559 fees (estimateFees) from the Sepolia RPC → sign real EIP-1559 tx in-browser → eth_sendRawTransaction. "Send on Sepolia" UI (editable RPC URL default https://ethereum-sepolia-rpc.publicnode.com, recipient, amount, live balance, txid+Etherscan on success). VERIFIED live: browser reached the real node (eth_chainId→0xaa36a7), read real balance (0 ETH), broadcast returned the NODE'S OWN "insufficient funds for gas...balance 0" — proving the full path hit the actual chain. Only funding via a Sepolia faucet is left to land a confirmed tx. BUG FIXED en route: `HttpJsonRpcTransport` stored a bare `fetch` ref → "Illegal invocation" in browsers; now binds globalThis.fetch (chains 69 tests green). EVM broadcast = DONE. NEXT for "sabka kro": SOL devnet (build transfer message + getLatestBlockhash + signSolanaTransactionMessage → sendTransaction) and BTC testnet (getUtxos → build+sign PSBT via @scure/btc-signer/core signPsbt → broadcast) — each a real per-ecosystem tx-construction piece + needs a funded testnet account. — earlier wallet note below.**

**REAL NON-CUSTODIAL WALLET NOW LIVE (2026-07-06, commit fc55559): `apps/web/src/wallet.ts` runs `@intent-wallet/core` IN THE BROWSER (core is pure @noble/@scure — browser-safe; aliased to source in vite+tsconfig). WalletManager over a localStorage SecureStore → user can CREATE a wallet (real BIP-39 mnemonic generated in-browser → sealed vault scrypt+AES-256-GCM at localStorage['iw.vault.v1'] → persisted), IMPORT a phrase, UNLOCK/LOCK (15-min auto-lock), BACK UP the recovery phrase, and it does REAL EIP-1559 signing (a "sign a testnet tx" button produces a real 0x02 Sepolia/chainId-11155111 signed tx). Keys NEVER touch the server (non-custodial literal). WalletPanel UI replaces the server demo IdentityCard; shows the user's OWN real 3-address identity. VERIFIED live: create→phrase→vault→identity→real signed tx→reload=LOCKED→unlock restores same wallet. So "real wallet creation + seed gen + backup + testnet-ready signing" = DONE. Still needs a real RPC to BROADCAST (packages/chains/registry.ts already has public testnet RPCs — Sepolia/base-sepolia/etc. — + HttpJsonRpcTransport, so broadcast is a small follow-on) and testnet capability profiles + non-EVM step signers for BTC/SOL broadcast. — earlier LATEST below.**

**LATEST (2026-07-06, supersedes everything below): 30 workspace packages, ~756 tests. Added since the block below: (1) `@intent-wallet/sdk` (#30, commit 7f2b8a7) — zero-dep typed `/v1` client (fluent createClient + functional core + typed ApiError + injectable transport + retry-GET-only); web app DOGFOODS it (d8b9a13); docs arch 35 + ADR-0052 (5ce6b5c); SDK adversarial review INCOMPLETE (session-limit) → RE-RUN needed. (2) AI-NATIVE WEB REDESIGN (89b9791) — ChatGPT+Apple-Wallet feel: hero, chat bubbles, portfolio CARDS, the AI PLANNING STEPPER (Understood→Security→Route→Cost→Authorize→Execute from real data), route graph. (3) UNIVERSAL IDENTITY 3-address (3da8cd2) — GET /v1/identity derives REAL BTC+EVM+SOL addresses via core HDKeyring (BIP-32/44/84) from the public test-vector seed (demo; prod derives on-device); UI "one identity, three chains" card. (4) REAL pre-broadcast SIMULATION (demo StepSimulator, sandbox) + session ACTIVITY history surfaced in UI (9193893). USER PRODUCT STEER (P0): wants ChatGPT+Stripe+Apple-Wallet UX (done). User chose to build identity(✓)+sim/history(✓)+real-signer/RPC-seams(✓ DONE, commit ae73f54). #80 = `packages/runtime/src/live.ts` (30 packages, ~761 tests): `createEvmStepSigner` builds a REAL EIP-1559 tx from a plan step (proven byte-identical to a direct core sign) + `EvmDevice` key-holding seam (createLocalEvmDevice for tests; WalletConnect/hardware in prod — key never crosses the seam) + `createLiveExecutor` (real signer + AdapterChainGateway + gas planner) + `discoverHoldings` (multi-chain balance discovery). Arch 36 + ADR-0053. TO GO LIVE END-TO-END the deployment injects THREE things: ① an EvmDevice (WalletConnect project id / hardware / on-device keyring), ② an RPC-backed BlockchainAdapter (real RPC URL), ③ a nonce source (eth_getTransactionCount) — that's the only remaining gap; all runtime code is built + offline-tested. NO fake P/L/savings (deferred to historical price + router-comparison sources). — Rest of this block is historical detail.**

**CURRENT (this rewrite supersedes the stale prose below): DESIGN ROADMAP #1–#30 COMPLETE (arch docs 01–34 + ADRs 0001–0051). 29 workspace packages, ~740 tests green. NEW: `packages/capabilities` (26 tests, arch 34, ADR-0051, commits f999aad/b6a71e1/43aa495) — the deterministic "what can this chain/provider do?" registry (versioned ChainCapabilityProfile keyed on CAIP-2, static route classes, fail-closed CapabilityService) WIRED into WalletRuntime.plan() as a feasibility gate that rejects a stake-on-a-non-staking-chain / bridge-to-an-undeclared-route BEFORE authorize/sign. Adversarial review re-run: 15/18 confirmed findings fixed (feeModelFor fail-closed, retire-in-place isolation, validation gaps for label/effectiveFromIso/nativeAsset.name/finality/null-subobjects, removed the bridge self-loop fallback + prefer per-step params). The wallet is WIRED + RUNNING: `packages/runtime` composition root wires the real engines → NL→plan→authorize(policy)→execute(device-sign→broadcast→confirm) runs end-to-end, and the GAS ENGINE is now wired into execute (sponsorship + bounded EIP-1559 + fee-token per EVM step; commit fa24381); `services/api` exposes the WHOLE loop over HTTP — `POST /v1/intents/{plan,authorize,execute}` + `GET /v1/portfolio` (authorize/execute act on a server-issued plan by planId; execute re-authorizes server-side + is dev-gated to a wired executor); `apps/web` (React) walks the full loop in the browser (plan → authorize verdict → execute result, verified live). PRODUCTION BUILD GREEN (`pnpm -r build` cold, 28 projects + `node dist/main.js` runs, no tsx). Servers: api :8080, web :5173. Plugins adversarial review RE-RUN + 9 findings fixed (commit d29c410); gas engine wired into execution (fa24381). Remaining (needs external resources, honestly deferred): real vendor/RPC + device-signing + live fee-market source (stubs today), real paymaster/bundler, mobile app, external security audit, perf cert, Phase-3 intelligence live sources. Doctrine intact: AI/plugins/agents propose, deterministic code verifies, device signature disposes; non-custodial throughout. — Older status below is historical.**

**[HISTORICAL] 26 packages built and green (683 tests) + POLICY GATE wired (runtime.authorize: intent→policy→sign, composed with the same RiskEngine); services/api exposes POST /v1/intents/plan wired to the runtime + the FIRST composition root (packages/runtime) wiring real engines into a runnable NL→plan flow. The core engines are DONE as standalone, offline-testable IP:** core (wallet+signing), chains (adapters), intents (NL→plan), execution (step machine), providers (aggregator framework), router (route optimizer), risk (immune system), intelligence (financial brain), policy (zero-trust authorization gate), copilot (AI decision layer), automation (autonomous workflows), settlement (Stripe-of-Web3 orchestration), reliability (SRE brain), scale (bounded autoscaler + resilience + multi-region + cache), identity, portfolio (aggregation), + foundation (config/observability/events/ui) + services/api skeleton. Design Track #1–#25 are ✅.

**Hardening status:** CRITICAL#2 (planner graduated-risk) DONE (commit 981212d). CRITICAL#1 (stale-plan) now CLOSED AT THE ORCHESTRATION LEVEL by the Settlement Engine's MANDATORY pre-flight stage (settlement is the front door to execution; a stale plan parks before broadcast — proven by test). Still open (lower priority): the execution-engine INTERNAL broadcast-idempotency (settlement adds settlement-id idempotency on top, but execution's persist-before-broadcast is still a belt-and-suspenders TODO), native max-send gas reserve (intents resolve.ts), requote recovery inside execution driver, RiskReport→PlanRiskSummary rename, reconcileCitations extract.

**Full autonomy spine complete:** AI (copilot) proposes → automation (workflows) fires → Risk AND Policy authorize → device/session-key signs → execution. Automation delegates authorization to Policy (never re-implements it) and holds no keys, so an automated action is provably no more capable than a manual one. **Next per user's explicit steer: #19 = Developer Platform / Public APIs & SDKs (expose the standalone engines as products), NOT more wallet features. Also strongly recommended by the user: a Capability Registry (which chain/provider/feature supports what — staking? atomic swap? bridge health?) so Intent Engine + Route Optimizer decide on dynamic capabilities, not hardcoded assumptions.**

**The full pre-execution gate now exists:** AI (copilot) proposes → Risk (is it dangerous?) AND Policy (is the user authorized?) must BOTH pass → device signature disposes. The copilot has no execute capability; the policy engine holds no keys. Automation (#18) will run through the SAME gate, so an automated action can never do what a manual one couldn't.

**The engines exist but nothing is WIRED to reality yet.** Every engine is a pure core over injected interfaces (ADR-0030). Those interfaces are still fakes. The user's guidance: after ~#20 shift to 80–90% implementation. The next phases turn the interfaces into real, running software. In priority order:

1. **Real vendor plugins** (Prompt #13/#14 leftovers) — implement the `providers` interfaces against live vendors behind the plugin boundary: swap (0x / 1inch / Jupiter), bridge (LiFi / Across), price (CoinGecko / on-chain TWAP), gas (per-chain), simulation (Tenderly / eth_call). Each is one plugin file + fixture tests; NO hardcoding in execution.
2. **Real `StepDriver`** — the missing seam that makes execution actually move funds: wire `core.WalletSigner` + `chains.AdapterRegistry` + `router` (route) + `risk` (authorize) + the vendor plugins. AI proposes → risk+deterministic verify → device signs → driver broadcasts.
3. **Phase 3 finish — Portfolio + Intelligence live wiring** — real `BalanceSource`/`PriceSource` (chains + price plugin) for aggregation, and `PositionSource`/`PriceHistorySource`/`SnapshotStore`/`MarketEventFeed`/`AssetCatalog` for the intelligence engine (LP/staking/lending discovery, history for TWR/drawdown, market-event feed for alerts). Makes portfolio + brain show real money.
4. **Phase 7 — Backend Platform** (`services/api`) — expose intent/plan/route/risk/portfolio/intelligence over the API; the real integration surface where all engines meet. This is what a client app (Phase 8) and the public SDK (Prompt #19/#20) consume.

Per the user's roadmap the likely next pasted prompt is **#16 (AI Financial Copilot — real Claude `LlmClient` for intents + the `Narrator` boundary in intelligence is already anti-fabrication-guarded)**, then #17 Automation, #18 Payment Layer, #19 SDK, #20 Dev Platform, #21 White-label, #22 Solver Network, #23 Enterprise, #24 Observability/SRE, #25 Launch. Business model the user is leaning toward: **HYBRID** (wallet users + infra/API/SDK developers + enterprise) — the standalone engines (router, risk, intelligence) each become a separate revenue stream. Build the pasted prompt; otherwise start at item 1 above.

## 🧱 What exists right now (code inventory)

```
FOUNDATION (Bootstrap, Prompt #6) — all tests green, typecheck+lint+format clean
packages/config/             ✅ 6 tests — Zod env loader, fail-fast, no raw process.env elsewhere
packages/observability/      ✅ 12 tests — structured logger + secret redaction, AppError hierarchy, problem+json
packages/events/             ✅ 12 tests — event envelope, Kafka topic registry, Redis key registry, payload schemas
packages/ui/                 ✅ 9 tests — cross-platform design tokens (color/space/type/radius/motion) + CSS-var emitter
services/api/                ✅ 10 tests — Fastify app factory: request-context+requestId, error→problem+json,
                             /healthz+/readyz, /v1 versioning, OpenAPI introspection, FAIL-CLOSED auth guard skeleton
Standards/infra: eslint.config.mjs (catches real bugs), .prettierrc, .editorconfig, .githooks (commit-msg+pre-commit
  via core.hooksPath), scripts/validate-commit-msg.mjs (tested), .github/workflows/ci.yml, infra/docker/api.Dockerfile,
  docker-compose.dev.yml (PG+Redis), infra/k8s/base/*.yaml, core coverage gate (90% floor, measured 98%)

packages/intelligence/       ✅ COMPLETE (Prompt #15 — Portfolio Intelligence, the FINANCIAL BRAIN, STANDALONE) — 38 tests
  src/types.ts               money-vs-ratio rule; Position(8 kinds; borrowing=debt) + PortfolioSnapshot + PortfolioIntelligence
  src/stats.ts               pure quant primitives (mean/variance/stdev/returns/pearson/covariance/beta/hhi/drawdown)
  src/positions.ts           normalize: net worth = assets − debt; classify (built-in or injected); asset weights
  src/allocation.ts          allocation (asset/sector/chain/protocol/liquidity) + HHI concentration
  src/performance.ts         unrealized PnL (cost-basis) + time-weighted return (flow-adjusted) + volatility + drawdown
  src/risk.ts                diversification (correlation ratio, HHI fallback) + leverage + explainable health score [0,100]
  src/insights.ts            deterministic rules w/ evidence + policy presets (conservative/balanced/aggressive)
  src/alerts.ts              stateful dedup/cooldown alert engine over analytics + injected MarketEventFeed; now passed in
  src/scenario.ts            what-if: price shock (LP impermanent-loss √r, β-propagation), gas cost, bridge outage
  src/tax.ts                 lot matching FIFO/LIFO/HIFO/AVERAGE, short/long term, jurisdiction-abstracted, exact bigint
  src/narrator.ts            AI narration boundary: TemplateNarrator + verifyNarrative (cites ONLY verified numbers)
  src/engine.ts              PortfolioIntelligenceEngine facade: analyze/scenario/evaluateAlerts/tax/narrate
  src/sources.ts             injected PositionSource/PriceHistorySource/SnapshotStore/MarketEventFeed/AssetCatalog
  ANALYZES + RECOMMENDS, never signs/executes. AI narrates verified facts only → rejects fabrication. Depends on portfolio (money math)

packages/risk/               ✅ COMPLETE (Phase 6 — the immune system, STANDALONE) — 20 tests
  src/scoring.ts             composite score = 1 − Π(1−severity) (probabilistic-OR) + level + hard-block
  src/detectors.ts           pure heuristics (honeypot/unlimited-approval/fresh/illiquid/concentration/admin/unaudited/poisoning)
  src/intel.ts               ThreatIntel interface + InMemory (sanctions/blacklist/scam-token/malicious/phishing; signed-snapshot shape)
  src/policy.ts              PolicyEngine + presets (strict/balanced/permissive); block never overridable
  src/engine.ts              RiskEngine facade: scan()→report, evaluate()→SecurityDecision (allow/confirm/block)
  fills the RiskProvider intents/execution/router expect (map report.level+signals→{level,reasons})

packages/router/             ✅ COMPLETE (Prompt #13 — Global Route Optimizer, STANDALONE) — 17 tests
  src/scoring.ts             the IP: 7-factor weighted scoring (min-max normalized) + presets (balanced/cheapest/fastest/safest)
  src/candidates.ts          CandidateGenerator (all providers → normalized RouteCandidate) + simulateCandidates
  src/predictor.ts           RoutePredictor ML boundary + boundedPredictor (±band clamp) + identityPredictor
  src/optimizer.ts           GlobalRouteOptimizer: discover→simulate-gate→score→ML-rerank→best+alternatives+confidence
  src/{types,request,errors}.ts
  depends ONLY on providers (standalone → public-API infra product)

packages/providers/          ✅ COMPLETE (Phase 5 — Aggregator Framework) — 16 tests passing
  src/provider.ts            Swap/Bridge/Price/Gas/Simulation plugin interfaces + quote types
  src/health.ts              HealthTracker: composite score (success+latency) + circuit breaker
  src/registry.ts            ProviderRegistry: run(failover) + collect(aggregation) by score
  src/aggregate.ts           isValidSwapQuote (reject stale/invalid) + bestSwapQuote (best of N)
  src/route.ts               RouteOptimizer (swap same-chain / bridge cross-chain) → fills intents RouteProvider
  test/                      health scoring, circuit open/half-open/close, failover, aggregation, route

packages/execution/          ✅ COMPLETE (Phase 5) — 12 tests passing
  src/state.ts               Execution/StepState + status transitions + runnable/ordering helpers
  src/driver.ts              StepDriver interface (simulate/broadcast/confirm/verify) + DriverError
  src/store.ts               ExecutionStore + InMemoryExecutionStore (resumability)
  src/events.ts              ExecutionEvent union + EventSink
  src/engine.ts              ExecutionEngine — step machine (execute/resume), sandbox-gate, retries, park
  test/                      happy/sandbox-halt/retry/park/resume/ordering/invariant (scripted fake driver)

packages/intents/            ✅ COMPLETE (Phase 4 — THE MOAT) — 40 tests passing
  src/schema.ts              Intent + Amount + ExecutionPlan (Zod, versioned)
  src/amount.ts              amount parse ($/₹/crypto/all/half/%) + exact decimal↔base
  src/parse/deterministic.ts rule-based fast-path (all flagship utterances)
  src/parse/parser.ts        CompositeParser (deterministic→schema-validated LLM) + LlmClient boundary
  src/plan/context.ts        injected interfaces (holdings/prices/routes/risk/recipient/fees) + MapHoldings
  src/plan/resolve.ts        Amount→base units (float-free; USD fiat)
  src/plan/planner.ts        planIntent→PlanOutcome (safety checks + confirmation)
  src/engine.ts              IntentEngine facade (parse+plan)

packages/chains/             ✅ COMPLETE (Phase 2) — 69 tests passing
  src/registry.ts            12 chains (EIP-155 ids, decimals, finality, default RPCs — pinned)
  src/provider.ts            ProviderPool (priority failover, cooldown, timeout, revert≠outage,
                             API-key redaction) + HttpJsonRpcTransport
  src/adapter.ts             BlockchainAdapter interface + FeeEstimate/TxStatus/AssetBalance types
  src/adapter-registry.ts    AdapterRegistry — THE gateway (ChainId→adapter, keyed URLs first, memoized)
  src/evm/{abi,adapter}.ts   EvmAdapter: balances, EIP-1559 fees, gas/nonce/simulate, broadcast, status
  src/solana/adapter.ts      SolanaAdapter: lamports, SPL, priority fees, broadcast, sig-status
  src/bitcoin/{rest,adapter} HttpRestTransport (esplora) + BitcoinAdapter: UTXO balance/utxos, fees, broadcast
  test/                      all adapters tested with scripted fake transports (exact RPC/REST asserted)

packages/core/               ✅ COMPLETE + Wallet Core (Phase 1 + Prompt #7) — 126 tests, coverage 97.8%/92.8% branch
  [primitives — Phase 1]     mnemonic, slip10, keyring (Universal Identity), vault, accounts/{evm,bitcoin,solana}
  src/signing/rlp.ts         minimal in-repo RLP (spec vectors)
  src/signing/evm-transaction.ts   EIP-1559 tx sign/serialize (byte-identical to viem, recoverable)
  src/signing/evm-typed-data.ts    EIP-712 hashTypedData + signTypedData (matches viem incl. arrays/structs/bytesN/int)
  src/signing/bitcoin-psbt.ts      PSBT signing via @scure/btc-signer (finalize/extract; multi-party no-op safe)
  src/signing/solana-transaction.ts serialized-message signing
  src/signing/signer.ts      SigningManager implements WalletSigner (derive→sign→zeroize per op)
  src/wallet/secure-store.ts SecureStore interface + InMemorySecureStore (native impls in apps/mobile)
  src/wallet/session.ts      SessionManager auto-lock (injectable scheduler)
  src/wallet/wallet-manager.ts  WalletManager facade: create/import/unlock/lock/changePassword/wipe/getSigner
  @scure/btc-signer now RUNTIME dep; viem is DEV-only oracle (like ed25519-hd-key)
  src/bytes.ts               hex/bytes/zeroize/constant-time helpers
  src/errors.ts              typed error hierarchy (WalletError codes)
  src/mnemonic.ts            BIP-39 generate/validate/toSeed (@scure/bip39)
  src/slip10.ts              SLIP-0010 ed25519 HD derivation (own impl, spec vectors)
  src/accounts/evm.ts        secp256k1 → keccak → EIP-55 address + sign/recover digest
  src/accounts/bitcoin.ts    BIP-84 P2WPKH bech32 (mainnet/testnet)
  src/accounts/solana.ts     SLIP-0010 → ed25519 → base58 + sign/verify message
  src/keyring.ts             HDKeyring: mnemonic → UniversalAccount {btc, evm, sol}
  src/vault.ts               scrypt + AES-256-GCM versioned envelope, tamper-proof
  src/index.ts               public API
  test/*.test.ts             official vectors: BIP-39 (Trezor), BIP-84, EIP-55,
                             SLIP-0010 spec vectors, vault roundtrip/tamper, property tests
Root: package.json (lint/format/prepare scripts), pnpm-workspace.yaml (packages+services+apps),
  tsconfig.base.json (+ paths for @intent-wallet/* → src), .gitignore, CONTRIBUTING.md, README.md
```

**Cross-package resolution (important):** tsconfig `paths` + per-consumer vitest `resolve.alias` map `@intent-wallet/*` → package `src/index.ts`, so typecheck & tests need NO prior build. Production images run `pnpm -r build` (dist) then run compiled JS. `pnpm-workspace.yaml` MUST include `services/*` (was a bug — fixed).

## 🧠 Decisions Log (index → canonical ADRs in [docs/adr/](docs/adr/README.md))

**All decisions are now formalized as ADR-0001…0028** in [docs/adr/](docs/adr/README.md) (Context · Decision · ≥3 Alternatives · Maintenance/Scaling/Security consequences). The D-numbers below are the legacy index; the ADRs are canonical. No implementation deviates from a locked ADR without a superseding ADR.

| #       | Decision                                                                                                                                                                                          | Why / Tradeoff                                                                                                           | Date       |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | ---------- |
| D1      | TypeScript end-to-end, pnpm monorepo                                                                                                                                                              | shared types core↔client↔server; Rust deferred to solver-at-scale                                                        | 2026-07-05 |
| D2      | @noble/@scure crypto suite (zero-dep, audited)                                                                                                                                                    | vs bitcoinjs/ethers internals: smaller supply-chain surface, RN-friendly (pure JS)                                       | 2026-07-05 |
| D3      | SLIP-0010 ed25519 implemented in-repo (~60 LOC)                                                                                                                                                   | `ed25519-hd-key` pkg is stale w/ legacy deps; own impl validated against official SLIP-0010 test vectors + cross-checked | 2026-07-05 |
| D4      | Vault: scrypt (N=2^15,r=8,p=1 default) + AES-256-GCM, params in versioned envelope                                                                                                                | argon2id lacks solid pure-JS/RN story; params-in-envelope enables KDF upgrades without breaking old vaults               | 2026-07-05 |
| D5      | Address indexes: BTC/EVM non-hardened last segment, SOL fully hardened `m/44'/501'/i'/0'`                                                                                                         | matches ecosystem conventions (Ledger/Phantom/MetaMask compatibility)                                                    | 2026-07-05 |
| D6      | EVM signatures return 65-byte r‖s‖v with low-s normalization                                                                                                                                      | malleability protection; recovery bit needed for ecrecover flows                                                         | 2026-07-05 |
| D7      | viem (EVM), @solana/kit (SOL), @scure/btc-signer (BTC) for Phase 2+                                                                                                                               | modern, typed, tree-shakeable vs ethers/web3.js legacy                                                                   | 2026-07-05 |
| D8      | Claude API with forced tool-use for intent parsing; deterministic fast-path first                                                                                                                 | structured output guarantees; cost/latency control; graceful degradation to forms                                        | 2026-07-05 |
| D9      | ProviderPool selects by configured priority + cooldown, NOT latency-weighted routing                                                                                                              | deterministic, no routing flap; keyed endpoints first, public fallbacks last; EWMA kept for observability only           | 2026-07-05 |
| D10     | JSON-RPC error responses (reverts etc.) never trigger RPC failover                                                                                                                                | deterministic chain answers — retrying on another endpoint burns rate budget and hides real errors                       | 2026-07-05 |
| D11–D20 | Bus (Redis Streams→Kafka), Postgres, React Native, TS backend + Rust budget, EKS, Claude via AI Gateway, ClickHouse, modular-monolith-first, ws-gateway fan-out, 4337 session keys for automation | full comparisons in [docs/architecture/09-decisions.md](docs/architecture/09-decisions.md)                               | 2026-07-05 |

## ⚠️ Known Constraints / Watch-outs

- Keys/seeds NEVER leave `packages/core`; server never signs (cardinal rule — requirements.md §4.1).
- All amounts are `bigint` — floats banned in money paths (lint rule to add in Phase 3).
- Quotes hard-expire 30s; never reuse stale quotes (requirements.md §4.6).
- JS `Uint8Array` zeroization is best-effort (GC copies possible) — documented in core; native secure memory on mobile in Phase 8.
- Project dir has a space ("INTENT LAYER") — always quote paths in shell commands.
- No git remote configured yet; repo is local-only. No CI runner yet (Phase 0 leftover → fold into Phase 7/10).

## 📅 Work Log

### 2026-07-05 — Session 18 (Architecture Review @ #20 checkpoint + Hardening Milestone)

At the #20 checkpoint the user asked (their "most important advice") for a holistic ARCHITECTURE REVIEW before more coding. Ran a 4-dimension multi-agent review (each read real source, cited file:line). **Verdict: architecture is FUNDAMENTALLY SOUND** — 19-package graph acyclic + cleanly layered, zero inversions, doctrine consistent. Full report: [docs/architecture/architecture-review-2026-07.md](docs/architecture/architecture-review-2026-07.md). User chose **"Hardening milestone first"**.

**Review key findings (confirmed vs source):** THE gap = no composition root; `services/api` wires ZERO engines; the engine→injected-interface ADAPTER TIER is unbuilt (only fakes). **5 adapters** needed for wiring (risk→intents RiskProvider subject-build+down-project [intents doesn't import risk]; portfolio UnifiedAsset→intents Holding amount→totalBase; portfolio PriceSource→intents PriceProvider getPrices→getUsd; 3 Route shapes normalizer; RiskReport rename). **🔴 CRITICAL:** (1) STALE-PLAN — plan-time checks never re-verified at broadcast (need mandatory pre-broadcast re-validation INSIDE execution); (2) PLANNER DROPPED GRADUATED RISK. **🟠 HIGH:** no broadcast idempotency (crash→double-send); native 'all'/percent send ignores gas reserve; requote RecoveryHint dead API; automation inner-action unchecked. **Duplication:** consolidate copilot.verifyResponse + intelligence.verifyNarrative → shared core reconcileCitations; risk+policy both export POLICY_PRESETS (footgun). Leave 3 condition ASTs + 2 envs (warranted). events package defined-but-UNUSED (execution emits a shape not matching ExecutionStepEventSchema) — decide wire-or-reserve.

**HARDENING DONE (commit 981212d):** CRITICAL #2 planner graduated-risk → new `ExecutionPlan.requiresStepUp` (medium/high sets it + surfaces reasons in confirmation; +2 tests); risk POLICY_PRESETS → `RISK_POLICY_PRESETS`. NOTE: `ExecutionPlan` now has a REQUIRED `requiresStepUp` field — any new plan fixture must set it.
**▶ HARDENING REMAINING (next):** (1) CRITICAL #1 execution pre-broadcast re-validation gate + broadcast idempotency (careful state-machine surgery — add a REQUIRED `StepDriver.preflight`; persist 'broadcasting' + a deterministic idempotency key BEFORE the network call so resume() can't double-send); (2) native max-send gas reserve (intents `resolve.ts` returns full totalBase for 'all' — deduct fee for native); (3) requote recovery (execution `#runStep` reads only `.retryable`, ignores 'requote'); (4) automation inner-action safety through the planner. Then low-sev: intents RiskReport→PlanRiskSummary, extract reconcileCitations to core. THEN composition root + 5 adapters (integration milestone), THEN resume features (#19 Developer Platform + Capability Registry, #20 Enterprise).

### 2026-07-05 — Session 17 (Prompt #18: Automation & Workflow Engine — the autonomous OS)

Built `packages/automation` — turns the wallet into an autonomous financial OS. **24 tests; 537 workspace total.** Designed via a 3-lens design-panel workflow (judge stalled on a session-limit hiccup; synthesized from the 3 designs + my own context). The ONE rule the user demanded: **automation is never unrestricted** — flow is User Rules → Policy → Risk → Execution, and automation NEVER bypasses it.

- **Pure deterministic orchestrator over injected sources** (clock via AutomationEnv, PriceFeed/PortfolioFeed/EventFeed via ContextProvider, PolicyAuthorizer=the gate, Executor=session-key signer, Notifier, WorkflowStore/RunStore). Holds no keys; authorizes nothing itself. Time injected → time-travel testable.
- **Typed Workflow language** (not a string DSL): Trigger union (schedule/price/price_move/portfolio/risk_event/volatility/chain_event/bridge_incident/gas/ai_recommendation/webhook), Condition AST (and/or/not + price/metric/gas/day/variable), Action union (swap/bridge/transfer/stake/unstake/claim/approve/notify/report/pause/disable/execute_intent).
- **NL compiler:** deterministic templates (DCA, buy-the-dip/stop-loss, scheduled reward claim, exploit-exit) + injected WorkflowLlmClient fallback. Compiles the user's exact example sentences.
- **Run pipeline (the gate proof):** trigger→conditions→safety→idempotency-claim→per financial action build PolicyRequest(policyType 'automation', automationRuleId=wf.id)→`authorizer.authorize` (Policy composes Risk)→ block→run blocked (never executes); !mayProceedToSign OR requireApproval→awaiting_approval (PARKED); allow+mayProceedToSign→`executor.execute` via sessionKeyId. 8 gate-proof tests: executes-small / parks-large($5k>biometric) / blocks-sanctioned / kill-switch / paused / **idempotent (same instance runs once)** / **simulate authorizes but never executes/persists** / not-due-skipped.
- **Non-custodial:** automated exec uses a pre-authorized policy-bounded SESSION KEY (ADR-0028); engine never holds the master key. **Safety split:** scheduling-level (cooldown/daily-cap/timeout/kill-switch/pause-resume) here; authorization safety (limits/recipients/biometric) DELEGATED to Policy (not duplicated). **Reliability:** deterministic scheduler (nextFireTime/isScheduleDue over injected clock), missed-window fires once, idempotency key = hash(wfId:instance) prevents double-execution (DB unique(idempotency_key)).
- Docs architecture/21 + ADR-0040. Reused policy's stableHash + createPolicyEngine (as the authorizer in tests) — full integration test through the REAL policy+risk gate.

### 2026-07-05 — Session 16 (Prompts #16 + #17: AI Financial Copilot + Universal Policy Engine)

Built TWO packages this session (user re-sequenced: Policy = #17, but built FIRST since Copilot depends on it). **Design panel workflow first** (seam-map + 3-lens policy panel + 3-lens copilot panel + synthesis judge; 3 of 6 design agents hit transient API errors but the zero-trust policy design + 2 copilot designs + high-effort judge produced implementation-ready specs). Then implemented inline. Then **adversarial-review workflow** (5 attack surfaces → verify each finding). **506 workspace tests (+82).**

**`packages/policy` — Universal Policy Engine (#17), 55 tests, STANDALONE (deps only on risk).** The zero-trust authorization gate: "is the user AUTHORIZED?" (orthogonal to risk's "is it dangerous?"). Pure over an injected `PolicyEnv` (clock/ids/hash) — the evaluator reaches NO Date.now/Math.random/crypto (source-grep + env-swap tests prove it). Typed rule model (`ConditionExpr` AST, not a string DSL) → RuleEngine → shuffle-invariant conflict resolution (hard-block → non-overridable floor → priority → most-restrictive → id) → 6-outcome decision → **composeWithRisk (most-restrictive-wins: Policy can only TIGHTEN Risk, block on either side terminal)** → ONE `ExecutionPermission` (mayProceedToSign is the only bool Execution reads; binds to planId for replay defense). Anti-spoofing: amount re-derived from injected planQuote, never the request. Presets strict⊇balanced⊇permissive + inheritance (child can't loosen a non-overridable parent, enforced at write AND resolve). Append-only hash-chained audit + verifyChain. Simulator (zero-writes dry-run) + admin (versioning/rollback) + metrics. architecture/19 + ADR-0038.

**`packages/copilot` — AI Financial Copilot (#16), 27 tests.** NOT a chatbot — a constrained DECISION layer. Pure orchestrator over INJECTED capabilities (no execute capability exists → can't move funds by construction). 4 guardrails as ARCHITECTURE: (1) tool registry scope read|analyze|propose, NO execute tool (assertNoExecuteTools + banned-name test), ProposedPlan.signed = literal false; (2) every figure a CitedFact verified vs a per-turn FactLedger (verifyResponse = generalized verifyNarrative) + hasUncitedNumerics prose scan; (3) any plan candidate FORCED through PolicyGate (composes Risk+Policy) — the LLM cannot make a plan 'ready'; block → explained_gate (plan nulled, no upgrade); fails closed with no policy wired; (4) confidence required + floored → uncertaintyNote. LlmClient boundary (utterance is a user msg, never in system prompt = injection defense) + deterministic ScriptedLlmClient. UserPreferences structurally secret-incapable (enums/symbol-regex only). architecture/20 + ADR-0039.

**The full pre-execution gate is now real:** copilot proposes → Risk AND Policy → device signs → execution. Next: #18 Automation & Workflow Engine (DCA / conditional rules), which runs through the SAME gate. Then real vendor plugins + StepDriver + Backend Platform to wire it all live.

### 2026-07-05 — Session 15 (Prompt #15: Portfolio Intelligence Engine — the financial brain)

- Built `packages/intelligence` — the biggest USP (MetaMask _shows_ a portfolio; this one _decides_). STANDALONE (depends only on portfolio for µUSD math) → Portfolio-Intelligence-as-a-service capable. **38 tests; 424 workspace total; all gates green.**
- **Two governing rules:** (1) it ANALYZES + RECOMMENDS, never signs/executes — every output is data + non-executable suggestions; (2) **AI never fabricates** — all numbers are computed deterministically, AI only narrates, and its narration is machine-verified (`verifyNarrative`) against the computed facts before display.
- **Money-vs-ratio discipline:** money = integer µUSD (bigint); ratios/scores/stats DERIVED from money (returns, vol, correlation, weights, health) = `number` — that's correct, a ratio isn't money. This is why net worth + tax reconcile to the penny while stats stay ordinary floats.
- **Pipeline** (`analyze()` is pure): Discovery→Normalization(net worth = assets − debt; borrowing subtracted)→Classification→Valuation→Allocation+Concentration(HHI)→Performance→Risk/Health→Insights.
- **Quant done for real** (not demo): time-weighted return (flow-adjusted, removes deposit timing), annualized volatility, running-peak drawdown, Pearson correlation, **correlation-adjusted diversification ratio** DR=(Σwσ)/σ_p (HHI fallback), explainable **health score [0,100]** = re-normalizing weighted blend of factors each returned with sub-score+weight+reason.
- **Four on-demand engines:** Insights (deterministic rules, each cites evidence metrics; policy presets conservative/balanced/aggressive) · Alerts (stateful dedup+cooldown, `now` passed in, over analytics + injected MarketEventFeed filtered to owned entities) · Scenario (price shock with **LP impermanent-loss √r / Balancer r^weight**, optional **β-propagation** to correlated assets, gas cost, bridge-outage trapped value) · Tax (FIFO/LIFO/HIFO/AVERAGE lot matching, short/long term, **jurisdiction abstracted to 3 params**, exact bigint proportional allocation, unmatched surfaced not guessed).
- **AI narration boundary:** `Narrator` interface + deterministic `TemplateNarrator` (no LLM, cites only what it read) + `verifyNarrative` gate → engine throws NARRATION_UNVERIFIED if any citation doesn't reconcile. The LLM narrator (#16) plugs in behind the SAME guard unchanged.
- **Injected sources:** PositionSource/PriceHistorySource/SnapshotStore/MarketEventFeed/AssetCatalog — all fakes for now; wiring them (LP/staking/lending discovery, history, event feed) is Phase 3 finish.
- **Docs:** `docs/architecture/18-portfolio-intelligence.md` (pipeline flowchart + narration-guard sequence, data model, formulas, API contract sketch, folder structure, <2s perf, roadmap) + **ADR-0037** (rejected LLM-computes-analytics / float money / HHI-only / built-into-portfolio / opaque-health). ADR index → 37. Mermaid valid.
- Fixed only: an `AlertContext` import location + one float-exactness test assertion. Clean build first try otherwise.
- **16 packages now.** User confirmed roadmap #16–#25 and is leaning **HYBRID business model** (wallet + infra APIs + SDK + enterprise) — the standalone engines each become a revenue stream. Next likely prompt: #16 AI Financial Copilot.

### 2026-07-05 — Session 14 (Prompt #14: Universal Security & Risk Engine — the immune system)

- Built `packages/risk` — every action evaluated BEFORE execution. STANDALONE (no deps) → security-as-a-service capable. Fills the `RiskProvider` intents/execution/router expect. **20 tests; 386 workspace total; all gates green.**
- **Pipeline:** subject (token/address/approval/provider) → threat-intel lookup (hard blocks) → heuristic detectors → composite score → configurable policy → SecurityDecision (allow/require_confirmation/block).
- **Detectors (pure, per-signal, each tested):** honeypot (sell-tax→hard block), unlimited-approval, fresh-token (age), low-liquidity (tiers), ownership-concentration, admin/upgradeable, unaudited, **address-poisoning** (lookalike of a saved contact: same first-6 + last-4, different middle).
- **Composite scoring:** probabilistic-OR `score = 1 − Π(1−severityᵢ)` (bounded [0,1], compounds many small risks); any hard signal (≥0.99: sanctioned/blacklisted/scam-token/honeypot) forces level=block regardless of score. Level thresholds 0.3/0.6.
- **ThreatIntel** injected interface (sanctions/blacklist/scam-token/malicious-contract/phishing) + in-memory impl; doc explains **signed-snapshot distribution** (verify before load → poisoned feed can't unblock a scam).
- **Policy Engine (configurable posture):** presets strict/balanced/permissive + custom (maxRiskScore, requireConfirmationAboveScore, blockUnaudited, blockUnlimitedApproval, minProviderHealth). **A block is NEVER overridable** — a permissive user cannot un-block a sanctioned address. Verdict order: hard-block → policy-block → confirmation → allow.
- **RiskEngine facade:** scan(subject)→report, evaluate(subject)→decision. Note: probabilistic-OR chosen over weighted-sum (bounds + compounds correctly); ML is a future bounded signal source, NOT the decision (explainable/auditable verdicts on money).
- **Docs:** `docs/architecture/17-security-risk-engine.md` (risk pipeline + scoring flowchart, per-subject signal table, policy config table, signed-intel distribution, incident response, threat model, <250ms perf) + **ADR-0036** (rejected blocklist-only/weighted-sum/ML-end-to-end/in-engine). ADR index → 36.
- Fixed: Cyrillic-А typo in a detector import; exactOptionalPropertyTypes on a test helper; 2 test premises (score compounding pushed riskyToken past balanced's max; address-poisoning lookalike length).
- **15 packages now.** Per user's roadmap: #15 Portfolio Intelligence (finish live wiring), #16 AI Copilot (real Claude LlmClient — intents.LlmClient boundary ready), then real vendor plugins + Backend Platform (Phase 7) to wire everything into services/api.

### 2026-07-05 — Session 13 (Prompt #13: Global Route Optimizer — the most valuable IP)

- Built `packages/router` — the "Google Maps of crypto". **STANDALONE by design** (depends only on `providers`) so it can be a public-API infra product powering third-party wallets. **17 tests; 366 workspace total; all gates green.**
- **Pipeline:** route discovery (ask ALL providers via collect) → candidate generation (normalize every quote into RouteCandidate) → **simulation gate** (reject routes that fail) → **deterministic weighted scoring** → **bounded ML re-rank** → best + alternatives + confidence.
- **Scoring engine (the crown-jewel IP, pure/testable):** 7 factors min-max normalized to [0,1] (output/cost/slippage/time/reliability/risk/freshness), combined by weights. **Dynamic weighting** via presets: balanced/cheapest/fastest/safest — each provably changes the winner (tested). `scoreCandidates` is a pure function.
- **ML separation (hard boundary):** `RoutePredictor` enters ONLY as a bounded re-ranker; `boundedPredictor` clamps every adjustment to ±band so ML can break ties but CANNOT crown a clearly-worse route (both directions tested). ML never signs/executes/bypasses simulation. Default = `identityPredictor` (pure deterministic).
- **Output:** RouteResult { best, alternatives (=fallback routes for the execution engine), confidence (0.5·health + 0.3·margin + 0.2·simulated), weightsUsed }. Errors: NO_ROUTE / ALL_ROUTES_FAILED_SIMULATION.
- **This fills the Route Optimizer that intents.RouteProvider + execution requote-hook expect** — the loop from Prompt #10/#11 is now backed by real multi-candidate optimization. Note: the basic RouteOptimizer in `providers` is a simple composer; this is the real multi-factor optimizer.
- **Docs:** `docs/architecture/16-route-optimizer.md` (pipeline flowchart + ML-boundary diagram, scoring formula table, presets, simulation gate, confidence, caching, perf <300ms, standalone-public-API positioning) + **ADR-0035** (standalone + deterministic scoring + bounded ML; rejected quote-picker/ML-end-to-end/in-wallet). ADR index → 35.
- **14 packages now.** Per user: the project is becoming an infrastructure company (routing-as-a-service). Next roadmap: #14 Risk Engine (fills RiskProvider), #15 Portfolio finish, #16 AI Copilot (real Claude LlmClient), then real vendor plugins (0x/1inch/Jupiter/LiFi) + Backend Platform (Phase 7).

### 2026-07-05 — Session 12 (Prompt #11 completion: Provider/Aggregator Framework)

- Reconciled: the Execution Engine CORE was built session 11. Prompt #11's "AGGREGATOR FRAMEWORK" section (+ the user's explicit note: "plugin architecture, no hardcoded aggregators") was the missing piece — built it as `packages/providers`. **16 tests; 349 workspace total; all gates green.**
- **Plugin interfaces:** SwapProvider/BridgeProvider/PriceProvider/GasProvider/SimulationProvider — each a thin adapter over a vendor; `id`+`kind` only. Nothing downstream ever names a vendor.
- **HealthTracker:** per-provider success-rate + latency-EWMA composite score + **circuit breaker** (closed→open after N fails→half-open probe after cooldown→closed on success / re-open on probe fail). Injectable clock.
- **ProviderRegistry:** `run(op)` = healthiest available provider + automatic failover (NO_PROVIDERS / ALL_PROVIDERS_FAILED); `collect(op)` = fan out to all healthy for aggregation. Selection self-corrects via score — degrading vendor shed automatically, no config edit.
- **Quote aggregation + validation:** `isValidSwapQuote` rejects non-positive output / bad slippage / **stale** (>30s); `bestSwapQuote` picks max output, tie-break lower fee. Provider responses NEVER trusted blindly.
- **RouteOptimizer:** composes swap (same-chain) / bridge (cross-chain) registries → provider-native `Route`. **This fills the injected `RouteProvider` the intents planner + execution requote-hook already expect** (backend maps Route→intents.RouteProvider). Loop closed between planning and execution.
- **Docs:** `docs/architecture/15-provider-framework.md` (flowchart + classDiagram + sequence, health/circuit, aggregation, "how it fills injected interfaces" table) + **ADR-0034** (plugins over health-scored registries; rejected hardcoded-aggregator/meta-SDK/static-config). ADR index → 34.
- **13 packages now.** Next per user roadmap: #13 Route Optimizer (real vendor plugins: 0x/1inch/Jupiter/LiFi behind these interfaces) → #14 Risk Engine (fills RiskProvider) → Backend Platform (Phase 7, wires everything into services/api + real StepDriver using core.WalletSigner + chains.AdapterRegistry).

### 2026-07-05 — Session 11 (Prompt #11: Execution Engine — the transaction orchestrator)

- Built `packages/execution` — runs an approved ExecutionPlan (from intents) as a persisted, resumable step machine. **12 tests; 333 workspace total; all gates green.** Completes the layering: Intent(what) → Execution(how/order) → Adapters(per-chain).
- **State machine:** per step, strict dependency order → `simulate (sandbox) → broadcast (device-signed) → confirm → verify`. Execution status running→completed|parked|failed; step status pending/simulating/broadcasting/confirming/confirmed/failed/reverted. `nextRunnableStep` gates on dependsOn-confirmed.
- **Guarantees (all tested):** (1) **simulate-before-broadcast** — mismatch NEVER broadcasts, parks; (2) **idempotent retries** — retryable DriverError retries same step to maxAttempts; (3) **never strand funds** — unrecoverable → PARK with known fundsLocation + resume path; (4) **resumable** — state saved after EVERY transition, `resume(id)` continues from first unconfirmed step, confirmed steps never re-run (crash-resume test proves it); (5) **non-custodial** — signing inside injected StepDriver on-device, engine never sees a key; (6) **observable** — ExecutionEvent per transition.
- **Injected interfaces (ADR-0030/0033 pattern):** `StepDriver` (simulate/broadcast/confirm/verify + DriverError{retryable,recovery: retry|requote|park}), `ExecutionStore` (in-memory impl; real=Postgres), `EventSink`. Fully testable offline with a scripted fake driver.
- **Recovery policy:** retry (transient) / requote (quote expired — hook in place for Route Optimizer) / park (unrecoverable/mismatch/invariant/revert). Post-confirmation invariant (received≥min) → violation parks (funds moved but not as promised = never "done").
- **Docs:** `docs/architecture/14-execution-engine.md` (stateDiagram + sequence diagram, guarantees table, recovery policy, threat model, non-custodial invariant, concurrency) + **ADR-0033** (persisted step machine over injected driver; rejected Temporal/direct-signing/fire-and-forget). ADR index → 33.
- Fixed: exactOptionalPropertyTypes on finishedAt (guarded stamp); unused test var.
- **12 packages now.** Next: the injected interfaces the planner+engine expect need real impls — **Route Optimizer + Risk Engine (Phases 5/6)** + the real StepDriver (wires core WalletSigner + chains AdapterRegistry). Then Backend Platform (Phase 7) exposes it all via the API.

### 2026-07-05 — Session 10 (Prompt #10: Universal Intent Engine — THE MOAT)

- Built `packages/intents` — the decision engine that turns NL into a safe ExecutionPlan. **40 tests; 321 workspace total; all gates green.** Everything foundation-first: uses identity (RecipientResolution), portfolio (usdToMicros/formatUsd), and injected route/risk/holdings/price interfaces.
- **Schema (Zod, versioned):** Intent discriminated union (transfer/swap/buy/stake/rebalance/recurring/emergency_exit/query/clarify/unsupported) + Amount union (fiat/asset/all/fraction/percent) + full **ExecutionPlan** (steps w/ deps, quote {youSend/youReceiveMin/fee/slippage/eta}, risk, fallback, rollback, confirmation). Money = base-unit/micro-USD strings, no floats.
- **Parser:** deterministic fast-path (`parseDeterministic`) for ~all flagship utterances (Convert my BTC to ETH / Send $100 USDT to Rahul / buy $50 of SOL / move everything to stables / stake 5 SOL / buy X every Monday / exit if bitcoin drops 15% / wallet queries) — returns Intent | clarify | null(defer). `CompositeParser` = deterministic→LLM; **LLM output VALIDATED against IntentSchema** (schema-forced tool use), retry-once, then clarify; `LlmClient` is the AI-Gateway boundary (injected, faked in tests). No LLM → forms-fallback clarify. LLM error → clarify, never throw.
- **Planner (`planIntent`→PlanOutcome):** pure over injected `EngineContext` (holdings/prices/routes/risk/resolveRecipient/estimateFeeMicros/ids). Safety at plan-time: balance check, recipient **network-match** (can't send EVM asset to BTC address), route existence, risk (BLOCK unoverridable), non-USD fiat→clarify (no guessed FX). PlanOutcome = plan | clarify | automation | answer | rejected. buy→swap from held stable; rebalance→multi-leg swaps to USDC; recurring/emergency→automation; query→answer.
- **IntentEngine facade** ties parse+plan. **AI proposes, deterministic verifies, signature disposes** — engine structurally CANNOT execute.
- **Docs:** `docs/architecture/13-intent-engine.md` (pipeline, AI-vs-deterministic boundary + WHY diagram, ExecutionPlan schema, safety, threat model incl prompt-injection, perf) + **ADR-0032** (pure planner + PlanOutcome contract; ADR index → 32).
- Fixed: narrowed query detector to require a wallet-noun (so "what is the meaning of life" defers to LLM); cleaned a lazy-import hack in the planner.
- **11 packages now.** Next: wire real Route Optimizer + Risk Engine (Phases 5/6), then Execution Engine (Phase 5) — consumes ExecutionPlan + device signature to actually move funds.

### 2026-07-05 — Session 9 (Prompt #9: Blockchain Adapter Layer — the only chain gateway)

- Reconciled: started last session (EvmAdapter balances + ProviderPool). Completed the whole layer. **chains 34→69 tests; 281 workspace total; all gates green.**
- **Expanded `BlockchainAdapter` interface** to the full capability surface: getNativeBalance/getTokenBalances/getBlockHeight/getAssetMetadata/validateAddress/estimateFees/broadcastRawTransaction/getTransactionStatus. FeeEstimate is a discriminated union (evm/btc/sol). Adapters broadcast+track but NEVER build or sign.
- **EvmAdapter completed:** estimateFees (EIP-1559 via eth_feeHistory, percentile per speed, tip floored 1 gwei, maxFee=2·base+tip); EVM-only getNonce(pending)/estimateGas/simulateCall; broadcastRawTransaction; getTransactionStatus (receipt + confirmations, revert→failed); validateAddress; getAssetMetadata.
- **SolanaAdapter (new):** getBalance→lamports, getTokenAccountsByOwner→SPL (one call, filtered), getSlot→height, getRecentPrioritizationFees→median priority, sendTransaction(base64), getSignatureStatuses→confirmed/failed/pending, 32-byte base58 validate.
- **BitcoinAdapter + HttpRestTransport (new):** esplora REST (sibling of JSON-RPC transport, same error taxonomy). UTXO balance (funded−spent), getUtxos (for PSBT building), fee-estimates→sat/vByte per speed, POST /tx broadcast (validates txid), confirmations from tip−height, bech32+legacy validate per network. No tokens.
- **AdapterRegistry (the gateway/DI):** ChainId→adapter (ProviderPool for EVM/SOL, HttpRestTransport for BTC), keyed URLs before public defaults, memoized. This is THE single entry point for chain access — business logic never constructs transports.
- Added `@scure/base` + `@noble/hashes` to chains deps (SOL/BTC address validation, base58check).
- **Docs:** `docs/architecture/12-blockchain-adapters.md` (flowchart + classDiagram, folder structure, per-ecosystem notes, error-handling taxonomy, indexing-vs-adapter distinction, testing) + **ADR-0031** (adapter layer = only gateway; ADR index → 31).
- Phase 2 (Chain Abstraction Layer) now ✅. Next: Intent Engine (Phase 4) — will use AdapterRegistry + identity.resolveRecipient + portfolio.

### 2026-07-05 — Session 8 (Prompt #8: Universal Identity Engine + adapter layer + portfolio)

- Reconciled: the identity TRIPLE was already in core (`HDKeyring`). Prompt #8's new work = everything ABOVE it. Built 3 packages + the EVM adapter layer the user explicitly wanted. **+47 tests → 246 workspace total; all gates green.**
- **`packages/identity`** (22 tests): `classifyAddress` — one strict validator across BTC (bech32+bech32m+base58check legacy) / EVM (EIP-55, rejects broken mixed-case) / SOL (32-byte base58); `deriveUniversalIdentity` (account → identity + metadata, deterministic device-independent id = hash of address triple); `ContactBook` (address-keyed dedupe, verified marker, `resolveRecipient` → address|contact|ambiguous|not_found, NEVER guesses among same-name contacts). This is what the Intent Engine will call.
- **`packages/chains`** (now 34 tests): `BlockchainAdapter` interface + **`EvmAdapter`** — native (`eth_getBalance`) + ERC-20 (`eth_call balanceOf`) via ProviderPool; in-repo minimal ABI (hardcoded selectors, no keccak dep) incl. `decodeString` handling BOTH dynamic-string and legacy bytes32 (MKR-style) symbols; per-token failure = skip, not whole-call fail. Tested with scripted fake transports asserting exact ABI bytes. **This is the adapter layer the user asked for before the Intent Engine.**
- **`packages/portfolio`** (15 tests): pure `aggregatePortfolio` — merges same asset across chains (normalizes differing decimals UP, exact), keeps per-chain provenance, **float-free fiat math** (micro-USD bigint: `usdToMicros`/`assetValueMicros`/`formatUsd`), dust split (<$1), staleness propagation, defensive drop of negative/zero. `BalanceSource`/`PriceSource` injected interfaces (chain+price layers implement) → aggregation testable with fixtures, fetch pluggable/cacheable.
- **Docs:** `docs/architecture/11-universal-identity.md` (identity↔account mapping, folder structure, interface defs, 2 Mermaid diagrams, security: spoofing/replay/metadata/privacy, caching, testing) + **ADR-0030** (pure-engines-over-injected-sources layering; ADR index → 30).
- Fixed: added `@noble/hashes` as a direct identity dep; one confused test assertion. `assetKey` override recorded as the seam for a canonical (chain,address)→asset-id registry (replaces MVP symbol grouping) with no API change.

### 2026-07-05 — Session 7 (Prompt #7: Wallet Core completion — transaction signing + manager layer)

- Reconciled: the crypto primitives were already shipped (Phase 1). Prompt #7's genuinely-new work = REAL transaction signing + the manager/orchestration layer. Built it all, **126 core tests, coverage 97.8% stmt / 92.8% branch (90% gate passes)**.
- **Transaction signing (the big gap):**
  - EVM: in-repo RLP encoder + EIP-1559 tx signing (unsigned payload BYTE-IDENTICAL to viem; signed tx recovers to signer) + EIP-712 typed data (hashTypedData matches viem exactly incl. nested structs, arrays, bytesN, negative int) + EIP-191 personal_sign. viem added as DEV-only oracle.
  - Bitcoin: PSBT signing via @scure/btc-signer (moved to runtime dep — still @scure family, ADR-0003 OK); finalize/extract → raw tx + txid; multi-party "key owns no inputs" handled as a valid no-op (btc-signer throws instead of returning 0).
  - Solana: serialized-message signing wrapper.
- **Manager layer (the facade the app depends on):** SigningManager (unified `WalletSigner`, derive→sign→zeroize per op) · SecureStore interface + InMemorySecureStore (native Keychain/Keystore impls belong in apps) · SessionManager (auto-lock, injectable scheduler, deterministic tests) · WalletManager (create/import/unlock/lock/changePassword/wipe/getAccount/getSigner/exportMnemonic — the single public entry point).
- **Docs:** `docs/security/wallet-core-threat-model.md` (module architecture, folder structure, 4 Mermaid diagrams incl. data-flow trust boundary, 11-threat STRIDE table, attack scenarios, backup/lost-device/compromised-device recovery, secure-coding practices, implementation order) + **ADR-0029** (WalletManager facade + unified WalletSigner; added to ADR index → 29 ADRs).
- Bugs caught by gates & fixed: coverage branch gate correctly failed at 88.8% → added targeted edge tests (typed-data field types, manager error paths, PSBT via manager) to 92.8%; unused import; two `0x02${string}` viem type casts; vault min-N (2^13) in test.
- Extension seam recorded: hardware wallets + MPC/social recovery become new `WalletSigner` implementations without touching consumers (v2).

### 2026-07-05 — Session 6 (Prompt #6: Project Bootstrap — FIRST big code session since Phase 1/2)

- Built the real engineering foundation all future features sit on. **Everything validated green: 149 tests, typecheck clean, ESLint clean, Prettier clean.**
- New foundation packages (all with tests): `config` (Zod env loader, fail-fast, reports all errors at once), `observability` (structured logger + recursive secret redaction + circular-safe + AppError hierarchy + RFC-9457 problem+json that hides 5xx internals), `events` (envelope schema, 9 Kafka topics + partition keys, Redis key registry with TTL/group policy, payload schemas — base-unit-string amounts, no floats), `ui` (design tokens encoded from design/01 + CSS-var emitter, token-integrity tests).
- `services/api` Fastify foundation (tested via `app.inject()`, no socket): request-context+requestId echo, error handler→problem+json (5xx internals hidden), `/healthz`+`/readyz` (readiness probes), `/v1` versioning, OpenAPI 3.1 route introspection (never drifts), **fail-closed auth guard skeleton** (rejects all tokens until real SIWE/JWT lands M7 — honest, not a fake pass). Graceful SIGTERM shutdown in main.ts.
- Engineering standards made REAL (not prose): ESLint flat config (caught a genuine `prefer-const` bug in my own test); Prettier normalized 80 files + `format:check` gate; git hooks (`commit-msg`→Conventional-Commits validator, `pre-commit`→typecheck+lint) wired via `core.hooksPath` in `prepare`; coverage gate on `core` (@vitest/coverage-v8, 90% floor, measured 98.3%; added bytes.ts tests for the constant-time/zeroize helpers).
- Infra baseline: multi-stage non-root api Dockerfile, `docker-compose.dev.yml` (Postgres+Redis), k8s dev manifests (namespace/deployment/service with securityContext + probes).
- Docs: `docs/handbook/06-bootstrap.md` (file tree, dependency graph, build order, commands, checklist, DoD, + honest mobile-Expo-shell plan deferred because it needs an RN simulator to validate).
- Fixed two real bugs found by the strict gates: missing `services/*` in pnpm-workspace.yaml; `exactOptionalPropertyTypes` violation in observability error constructors.
- **Honest scope call:** did NOT scaffold the mobile native app blind (can't build/validate Expo+native here) — shipped its platform-agnostic piece (tokens) + a file-by-file plan instead. Tracked as an open bootstrap item, not silently skipped.

### 2026-07-05 — Session 5 (Design Track Prompt #5: Technology Selection & ADR Lock)

- Delivered `docs/adr/` — **28 formal Architecture Decision Records** + index/template README, per Prompt #5 (design only, lock the stack). One ADR per component the user listed, each with Context · Decision · ≥3 Alternatives (pros/cons table) · Consequences split into Maintenance / Scaling / Security:
  - 0001 monorepo/pnpm · 0002 TypeScript (+Rust budget) · 0003 @noble/@scure crypto · 0004 SLIP-0010 in-repo · 0005 vault scrypt+AES-GCM · 0006 React Native · 0007 Node+Fastify · 0008 Postgres/Aurora · 0009 Redis · 0010 Redis-Streams→Kafka · 0011 ProviderPool RPC · 0012 per-chain indexing · 0013 AI Gateway/Claude · 0014 intent parser (deterministic-first) · 0015 SIWE auth · 0016 APNs/FCM push · 0017 ClickHouse · 0018 OTel observability · 0019 S3 · 0020 AWS · 0021 EKS/Karpenter/KEDA/ArgoCD · 0022 GitHub Actions/Argo Rollouts · 0023 KMS/Secrets Manager/ESO · 0024 Terraform · 0025 Vitest/fast-check/Testcontainers/Detox/Playwright/k6 · 0026 Foundry+ERC-4337 · 0027 modular-monolith→services · 0028 session-key automation.
  - These are now CANONICAL: memory Decisions Log D1–D28 is just the index; ADRs are immutable-once-accepted (change = superseding ADR).
  - Added root `CONTRIBUTING.md` (thin pointer to handbook) + a table mapping the user's requested artifact names (PRD/ARCHITECTURE/ADR/API_SPEC/SECURITY/ROADMAP/CONTRIBUTING/DECISIONS) → where each lives here.
- Verified: 28 ADR files, index↔files consistent, all 28 ADR-to-ADR cross-links resolve (checked programmatically). `pnpm -r test` still 95/95.
- **Planning phase now effectively complete** — flagged to user that every remaining design prompt maps to an existing doc; Build Track (Phase 2) is the clear next move.

### 2026-07-05 — Session 4 (Design Track Prompt #4: Engineering Foundation)

- Delivered `docs/handbook/` — the engineering handbook for a 100-engineer team (design/process only, no feature code, per Prompt #4). 6 docs:
  - README (principles, per-directory purpose table mapping the user's roadmap names → our packages, "how this handbook is enforced") · 01 standards (naming, folders, code style, Conventional Commits, trunk-based branching, PR rules, SemVer, release process w/ rotating release captain) · 02 shared-libraries (Clean-Architecture dependency-direction diagram, THE non-custodial firewall spec, contract table for all 16 shared packages) · 03 environments+CI/CD (local/preview/staging/prod, secrets, env vars, feature flags + mandatory kill switches, pipeline diagram, rollback) · 04 quality (coverage tiers 90/85/80/70, perf budgets, security gates incl. float-in-money grep, docs + API-doc standards, global Definition of Done) · 05 roadmap+team (11 milestones mapped to build phases w/ M2 detail template, 10-role org, squad-owns-context collaboration model, RFC/ADR process).
- **Made it real, not just prose** — added working enforcement (all tested, build stays green):
  - `scripts/validate-commit-msg.mjs` — zero-dep Conventional Commits validator (hook + `--range` CI mode); TESTED: passes valid, fails bad-type/no-blank-line, ignores merge commits.
  - `.github/CODEOWNERS` (security team required on core/execution/risk/auth), `.github/pull_request_template.md` (DoD checklist + security-impact section), `.github/workflows/ci.yml` (commit-lint + typecheck + test + gitleaks; later gates documented as pending, not faked).
  - `.editorconfig`, `.prettierrc.json`/`.prettierignore`, `.env.example` (namespaced IW_* vars).
- All handbook Mermaid diagrams pass validation; `pnpm -r test` still 95/95 green.
- Reconciliation updated: Engineering Foundation now ✅; flagged that planning is ~4 prompts ahead of code and Build Track (Phase 2) is the recommended next move.

### 2026-07-05 — Session 3 (Design Track Prompt #3: UI/UX & Design System)

- Delivered `docs/design/` — complete product design, "Calm Money" language, per the user's Prompt #3 (design only, no code). 10 documents:
  - README (10 binding principles, platform conventions) · 01 tokens (color/type/spacing/radius/motion/haptics/icons — full token tables, WCAG-AA pairs) · 02 components (~25 components incl. the signature CommandBar, PlanCard, StepTracker, and the ConfirmSheet "trust boundary" — anatomy/variants/states/a11y each) · 03 navigation (4-tab IA, sheet-vs-push rules, deep links, state-dependent entry) · 04 onboarding screens S-01..S-10 (splash→create→backup→quiz→biometric/PIN→unlock→recovery) · 05 home screens S-11..S-18 (portfolio, asset detail, receive-3-addresses, send, QR, NFT, contacts, watchlist) · 06 intent screens S-20..S-26 (intent chat, plan confirm, execution progress, receipt, activity, notifications) · 07 settings S-30..S-38 (security center, devices, automation/session-keys, alerts, staking, developer mode) · 08 standards (validation rules, 5-state system, error→UI map, WCAG-2.2-AA a11y, i18n incl. Hinglish) · 09 journeys (7 end-to-end Mermaid journeys incl. scam-token, park/resume, lost-phone recovery).
  - Design decisions locked to architecture: ConfirmSheet is the one trust boundary everywhere; chains invisible above asset-detail; automation depth = authorization depth (session keys, honest UX); simulation-gated approvals; risk friction scales with level; forms fallback when LLM down is a _designed_ state.
  - All 9 Mermaid diagrams pass structural validation.
- **Reconciled the user's big pasted roadmap (#3–#12):** updated the Design-Doc Track table to show what's already shipped (Wallet Core, Universal Identity, Tech/ADR lock, Bootstrap all ✅ via existing code/docs) vs. genuinely pending — so future sessions don't rebuild finished work.

### 2026-07-05 — Session 2 (Design Track Prompt #2: System Architecture)

- Delivered `docs/architecture/` — complete production architecture for the 100M-user target, per the user's Prompt #2 (no code, design only):
  - README (principles, constraints, capacity model, Stage A/B/C evolution) · 01 overview (context/container/communication/event diagrams) · 02 service catalog (17 services: mission/data/API/events/scaling/failure/SLO each) · 03 data (ER diagram 18 entities, partitioning, Redis strategy, 9 Kafka topics, retention/PII map) · 04 flows (5 sequence diagrams: auth, portfolio, flagship intent execution, recurring-intent session keys, failure/park recovery) · 05 infra (multi-region, EKS/KEDA/Karpenter, DR table, rate-limit layers, secrets) · 06 security (trust zones, 14-threat STRIDE table, AI injection defenses, hash-chained audit) · 07 API spec (REST+WS+webhooks+SDK) · 08 repo structure + CI/CD · 09 decisions D11–D20 (alternatives scored) · 10 cost (beta $4k → 100M-user $1.2M/mo, RPC self-host crossover, LLM cost ladder) + 10×-bottleneck scaling roadmap.
- Key architectural commitments: non-custodial invariant (platform can never sign); modular-monolith-first evolution with Stage C service boundaries pre-drawn; single-writer-region execution sagas; outbox + idempotent consumers; automation depth = authorization depth (4337 session keys, honest UX for BTC/SOL round-trips).
- All 12 Mermaid diagrams pass structural validation (script in scratchpad).
- User's ~20-prompt roadmap adopted as the Design Track table above; Prompt #2 marked ✅.

### 2026-07-05 — Session 1 (bootstrap + Phase 0 + Phase 1 + Phase 2 start)

- Wrote requirements.md (full PRD: vision, personas, architecture, security, phase plan §14) and this memory.md.
- Phase 0 COMPLETE: pnpm monorepo, TS 5 strict (NodeNext, exactOptionalPropertyTypes etc.), Vitest 3, .gitignore, git init (branch `main`), base tsconfig, README.
- Phase 1 COMPLETE: `packages/core` implemented + tested (71 tests, all green first run):
  - BIP-39 mnemonic (12/24 words), validated against the official Trezor vector (fetched, not from memory).
  - BIP-32/44/84 secp256k1 derivation (@scure/bip32) + in-repo SLIP-0010 ed25519 (validated: official SLIP-0010 vectors 1 & 2 fetched from spec, all path depths, PLUS property cross-check vs `ed25519-hd-key`).
  - Universal Identity: BTC `bc1q…` (BIP-84 official vectors incl. pubkeys + cross-check vs @scure/btc-signer), EVM EIP-55 (all 8 spec vectors + abandon-mnemonic vector `0x9858EfFD232B4033E47d90003D41EC34EcaEda94` ✓ confirmed), SOL base58 (cross-checked).
  - Vault: scrypt+AES-256-GCM envelope v1 with AAD-bound metadata; tamper (ct/nonce/salt/params)/wrong-password/roundtrip/NFKD-unicode-password/DoS-params tests.
  - Signing: EVM digest sign (RFC6979, low-s, recovery id, recover() roundtrip), SOL ed25519 sign/verify; keyring destroy() lifecycle.
  - Integration test = Phase 1 exit criterion: create → seal vault → lock → unlock → identical identity → sign on 2 ecosystems.
- Phase 2 STARTED: `packages/chains` registry + provider layer (24 tests): see inventory above.
- Verified at session end: `pnpm -r test` → **95 passed / 0 failed** (71 core + 24 chains); `pnpm -r typecheck` clean; core `pnpm build` clean.
- Git: initial commit on `main` (no remote yet).
