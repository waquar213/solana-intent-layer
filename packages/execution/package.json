{
  "name": "@intent-wallet/execution",
  "version": "0.1.0",
  "private": true,
  "description": "Execution Engine: safely runs an approved ExecutionPlan as a persisted, resumable step machine. Simulate-gated, idempotent, never strands funds. Never holds keys.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@intent-wallet/intents": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
