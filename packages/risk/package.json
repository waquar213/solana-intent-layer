{
  "name": "@intent-wallet/risk",
  "version": "0.1.0",
  "private": true,
  "description": "Universal Security & Risk Engine — the platform's immune system. Threat intel, heuristic detectors, composite risk scoring, and a configurable policy engine. Evaluates and authorizes; never signs, never holds funds.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
