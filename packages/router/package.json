{
  "name": "@intent-wallet/router",
  "version": "0.1.0",
  "private": true,
  "description": "Global Route Optimizer — the routing intelligence layer. Standalone by design: generates, scores, simulates, and ranks execution routes across DEX/bridge aggregators. Designed to power third-party wallets via a public API.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@intent-wallet/providers": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
