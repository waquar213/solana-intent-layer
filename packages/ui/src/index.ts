/**
 * @intent-wallet/ui — the design system. Phase 8 fills in components (Button,
 * PlanCard, ConfirmSheet, …) per docs/design/02-components.md. The foundation
 * ships the platform-agnostic design tokens that every component and both
 * client platforms consume.
 */
export * from './tokens/index.js';
