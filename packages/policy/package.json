{
  "name": "@intent-wallet/policy",
  "version": "0.1.0",
  "private": true,
  "description": "Universal Policy Engine — the zero-trust authorization layer between AI, users and the Execution Engine. Deterministic rule evaluation, conflict resolution, composition with the Security Engine, tamper-evident audit. Authorizes; never accesses keys, never signs.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@intent-wallet/risk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
